name: 煙テスト（Smoke Test）

# PRとmainブランチへのpushで実行
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  actions: read

jobs:
  smoke-test:
    name: 煙テスト - 基本機能確認
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      # 1. コードをチェックアウト
      - name: コードをチェックアウト
        uses: actions/checkout@v4

      # 2. Node.jsをセットアップ
      - name: Node.js セットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # 3. 依存関係をインストール
      - name: 依存関係インストール
        run: npm ci

      # 4. ユニットテスト（カバレッジ付き）
      - name: ユニットテスト実行
        run: npm run test:unit:coverage

      # 4.5. カバレッジレポート保存
      - name: カバレッジレポートアップロード
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/
          retention-days: 30

      # 5. Playwrightブラウザをインストール
      - name: Playwrightブラウザインストール
        run: npx playwright install --with-deps chromium

      # 6. 煙テスト実行
      - name: 煙テスト実行
        run: npm run test:smoke

      # 6. テスト失敗時のアーティファクト保存
      - name: テスト失敗時のスクリーンショット保存
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-test-results
          path: |
            playwright-report/
            test-results/
          retention-days: 7

      # 7. テスト結果をコメント（PRの場合）
      - name: テスト結果をPRにコメント
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let comment = '## 🔥 煙テスト結果\n\n';

            if ('${{ job.status }}' === 'success') {
              comment += '✅ すべてのテストが成功しました！\n\n';
              comment += '基本機能は正常に動作しています。';
            } else {
              comment += '❌ テストが失敗しました\n\n';
              comment += '基本機能に問題がある可能性があります。\n';
              comment += 'アーティファクトからスクリーンショットを確認してください。';
            }

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });
