name: Grammar Data Quality Check

on:
  push:
    paths:
      - 'public/data/grammar/**/*.json'
      - 'scripts/validate_grammar_advanced.py'
      - 'scripts/analyze_grammar_data_quality.py'
  pull_request:
    paths:
      - 'public/data/grammar/**/*.json'
      - 'scripts/validate_grammar_advanced.py'
      - 'scripts/analyze_grammar_data_quality.py'

jobs:
  quality-check:
    name: 文法データ品質検証
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      
      - name: Run advanced grammar validation
        id: validation
        run: |
          echo "🔍 文法データの高度検証を実行中..."
          python scripts/validate_grammar_advanced.py
          RESULT=$?
          if [ $RESULT -eq 0 ]; then
            echo "✅ 検証合格"
            echo "validation_status=success" >> $GITHUB_OUTPUT
          else
            echo "❌ 検証失敗"
            echo "validation_status=failure" >> $GITHUB_OUTPUT
            exit 1
          fi
      
      - name: Run quality analysis
        if: always()
        run: |
          echo "📊 品質分析を実行中..."
          python scripts/analyze_grammar_data_quality.py
          if [ -d docs/quality ]; then
            echo "📝 品質レポートを生成しました"
          fi
      
      - name: Upload quality report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: grammar-quality-report
          path: docs/quality/
          retention-days: 30
      
      - name: Comment PR with quality report
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const reportPath = 'docs/quality/grammar_quality_report.md';
            
            if (!fs.existsSync(reportPath)) {
              console.log('品質レポートが見つかりません');
              return;
            }
            
            const report = fs.readFileSync(reportPath, 'utf8');
            const validationStatus = process.env.VALIDATION_STATUS;
            
            const emoji = validationStatus === 'success' ? '✅' : '❌';
            const statusText = validationStatus === 'success' ? '合格' : '不合格';
            
            const guidelineUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/blob/main/docs/guidelines/GRAMMAR_DATA_QUALITY_GUIDELINES.md`;
            
            const comment = [
              `## ${emoji} 文法データ品質検証: ${statusText}`,
              '',
              report,
              '',
              '---',
              `*この品質レポートは自動生成されました。詳細は[品質ガイドライン](${guidelineUrl})を参照してください。*`
            ].join('\n');

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });
        env:
          VALIDATION_STATUS: ${{ steps.validation.outputs.validation_status }}
