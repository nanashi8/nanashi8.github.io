# スコアボードのテスト方法

## テストの実行方法

### 1. 基本的なテスト実行

ブラウザのコンソールを開いて以下を実行:

```javascript
window.runScoreBoardTests()
```

これにより、以下の10種類のテストが自動実行されます:

1. **本日統計の取得** - 正答率と回答数の妥当性チェック
2. **累計回答数の取得** - 回答数が負でないことの確認
3. **定着数の取得** - 定着数と出題数の関係チェック
4. **定着率の計算** - 定着率の計算ロジックの検証
5. **詳細な定着率統計** - 3段階分類の整合性チェック
6. **定着予測統計** - 定着間近・長期記憶などのカウント検証
7. **目標達成情報** - 進捗率の妥当性チェック
8. **忘却アラート** - アラート数の妥当性チェック
9. **パーセンテージの一貫性** - 異なる計算結果の一致確認
10. **データ範囲の妥当性** - 上限値と論理関係の検証

### 2. 現在の表示内容の確認

```javascript
window.checkCurrentScoreBoardDisplay("translation")
```

パラメータ:
- `"translation"` - 和訳モード
- `"spelling"` - スペルモード
- `"reading"` - 読解モード

これにより、スコアボードに表示される全ての数値を確認できます。

## テスト結果の見方

### 成功時の出力例

```
=== テスト結果サマリー ===
✅ 成功: 10件
❌ 失敗: 0件
📊 合計: 10件
```

### 失敗時の出力例

```
=== 失敗したテスト ===
❌ 定着率の計算
   定着率の計算が不正確: 期待値=75%, 実際=80%
   データ: {retentionRate: 80, expectedRate: 75, ...}
```

## モバイル版のレイアウト改善

### 変更内容

1. **タブ切り替えボタンの追加**
   - モバイルでは左右の矢印ボタンでタブを切り替え
   - 縦スペースを節約し、スクロール量を削減

2. **レスポンシブ対応**
   - 768px以下のデバイスでモバイル版に自動切替
   - ウィンドウサイズ変更に対応

### 使い方

- **◀ ボタン**: 前のタブに移動（最初のタブで押すと最後のタブへ）
- **▶ ボタン**: 次のタブに移動（最後のタブで押すと最初のタブへ）
- **中央の表示**: 現在のタブ名を表示

### タブの順序

1. 📊 基本統計
2. 🎯 目標・進捗
3. 📜 履歴（和訳・スペルモードのみ）

## 検証済みの項目

### データの整合性
- ✅ 定着数 ≤ 出現数 ≤ 出題数
- ✅ パーセンテージの合計 = 100%（誤差±1%）
- ✅ 全ての数値が非負
- ✅ 正答率が0-100%の範囲内

### 計算の正確性
- ✅ 定着率 = (定着数 / 出現数) × 100
- ✅ 加重定着率 = (完全定着 + 学習中×0.5) / 出現数 × 100
- ✅ 3段階の内訳の合計が総数と一致

### 表示の一貫性
- ✅ 複数箇所で同じデータを表示している場合、値が一致
- ✅ モバイル/デスクトップで同じデータを表示

## トラブルシューティング

### テストが実行できない

```javascript
// テストモジュールが読み込まれているか確認
console.log(typeof window.runScoreBoardTests);
// "function" と表示されればOK

// 読み込まれていない場合は手動インポート
import('./tests/scoreBoardTests').then(module => {
  module.runScoreBoardTests();
});
```

### 特定のテストだけ失敗する

1. ブラウザのLocalStorageをクリア
2. ページをリロード
3. 再度テストを実行

```javascript
localStorage.clear();
location.reload();
```

### データが不正確

LocalStorageのデータを確認:

```javascript
// 進捗データの確認
const data = JSON.parse(localStorage.getItem('progress-data-v2') || '{}');
console.log(data);

// 特定のモードの統計
window.checkCurrentScoreBoardDisplay("translation");
```

## 開発者向け情報

### テストの追加

`src/tests/scoreBoardTests.ts` に新しいテスト関数を追加:

```typescript
function testNewFeature(): TestResult {
  try {
    // テストロジック
    return {
      name: 'テスト名',
      passed: true,
      message: '✅ 成功',
      data: {}
    };
  } catch (error) {
    return {
      name: 'テスト名',
      passed: false,
      message: `エラー: ${error}`,
      data: null
    };
  }
}
```

そして `runScoreBoardTests()` に追加:

```typescript
results.push(testNewFeature());
```

### モバイルレイアウトのカスタマイズ

`src/App.css` の以下のセクションを編集:

```css
@media (max-width: 768px) {
  .score-tabs-mobile {
    /* モバイル版タブナビゲーションのスタイル */
  }
  
  .tab-nav-btn {
    /* 矢印ボタンのスタイル */
  }
  
  .current-tab-name {
    /* タブ名表示のスタイル */
  }
}
```

## リリース前チェックリスト

- [ ] 全テストが成功することを確認
- [ ] モバイルでタブ切り替えが正常に動作
- [ ] デスクトップで全タブが表示される
- [ ] 各タブの数値が正確
- [ ] パーセンテージの合計が100%
- [ ] LocalStorageのサイズが適切（< 5MB）
