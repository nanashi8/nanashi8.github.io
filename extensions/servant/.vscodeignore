# Exclude source and test files from VSIX (runtime uses dist/)
src/**
tests/**

# Bundle output only (exclude stale compiled subfolders)
dist/**
!dist/extension.js
!dist/extension.js.map

# Build tooling
scripts/**

# Local workspaces / fixtures
test-workspace-learning/**
test-workspace-neural/**

# Local debug artifacts
test-violation.md
test-diagnostic.mjs

# Tooling / configs
*.ts
*.map
vitest.config.*
tsconfig*.json
eslint.config.*

# Repo/meta
.gitignore

# Docs that aren't needed at runtime
PHASE*_COMPLETION_REPORT.md
docs/**

# Exception: README links to this guide; keep it in VSIX
!docs/AUTOPILOT_DELIBERATION_GUIDE.md

# OS/editor noise
.DS_Store
.vscode/**

# Bundled extension does not need node_modules
node_modules/**
