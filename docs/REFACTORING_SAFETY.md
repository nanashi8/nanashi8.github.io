# 🔒 リファクタリング安全ガイド

**作成日**: 2025年12月11日  
**対象**: Phase 2リファクタリング以降

---

## 📋 目次

1. [今回発生した事故](#今回発生した事故)
1. [実装された防止システム](#実装された防止システム)
1. [安全なリファクタリング手順](#安全なリファクタリング手順)
1. [チェックポイントシステム](#チェックポイントシステム)
1. [トラブルシューティング](#トラブルシューティング)

---

## 🚨 今回発生した事故

### 事故1: リベース時のsrc/消失 (2025-12-11)

**発生状況**:
- リベース中断後に`git reset --hard origin/main`を実行
- origin/mainに`src/`が存在しない状態だった
- ローカルの完全な`src/`(116ファイル)が全て消失

**原因**:
- リベース前のバックアップなし
- リモートブランチの状態確認不足
- ハードリセットによる強制上書き

**復旧**:
- reflogから`972bfe1`を発見
- `git checkout 972bfe1 -- src/`で復元
- 最終的に`v1.18.0-before-phase2-refactor`にリセット

### 事故2: SimpleWord混入 (2025-10-06～)

**発生状況**:
- iOSアプリ「SimpleWord」がWebアプリリポジトリに混在
- 初回コミット(15eb559)から存在
- リファクタリング計画に記載なし

**原因**:
- 最初からモノレポ構成だった
- リポジトリ名が`nanashi8.github.io`でWebアプリ専用に見えた
- `.gitignore`でSimpleWordを除外していなかった

**対策**:
- `.gitignore`にSimpleWord完全除外ルール追加
- ローカルからSimpleWord削除
- Git管理から除外

---

## 🛡️ 実装された防止システム

### 1. Gitフックによる多層防御

#### Pre-commit Hook (既存)
- 型チェック実行
- Lint実行
- ビルドテスト

#### Pre-commit-refactor Hook (新規)
```bash
# リファクタリング専用の追加チェック
- src/ファイル数監視 (ベースライン: 100ファイル)
- 大量削除検知 (5ファイル以上で警告)
- 重要ファイル削除防止 (App.tsx, main.tsx等)
- ビルド成功確認
```

#### Pre-rebase Hook
```bash
# リベース前に自動実行
- 自動バックアップタグ作成
- src/存在確認とファイル数チェック
- バックアップログ記録
```

#### Post-rebase Hook
```bash
# リベース後に自動実行
- src/整合性チェック
- ファイル数検証
- 復旧ガイド自動表示(失敗時)
```

#### Pre-push Hook
```bash
# プッシュ前に自動実行
- 最終安全チェック
- SimpleWord混入検知
- 必須ファイル存在確認
```

### 2. チェックポイントシステム

**`scripts/refactor-checkpoint.sh`**:

```bash
# チェックポイント作成
./scripts/refactor-checkpoint.sh save phase2-hooks-extraction

# 現在の状態と比較
./scripts/refactor-checkpoint.sh compare

# チェックポイント一覧
./scripts/refactor-checkpoint.sh list

# チェックポイントに戻る
./scripts/refactor-checkpoint.sh restore phase2-hooks-extraction
```

**記録される情報**:
- コミットハッシュ
- ブランチ名
- src/ファイル数
- components/ファイル数
- hooks/ファイル数
- features/ファイル数
- ビルド状態

---

## ✅ 安全なリファクタリング手順

### Phase 2以降の推奨ワークフロー

#### ステップ1: 準備
```bash
# 現在の状態を確認
git status
npm run build
npm test  # (テストがあれば)

# Phase開始前のチェックポイント作成
./scripts/refactor-checkpoint.sh save phase2-start
```

#### ステップ2: 小さく分割して実施
```bash
# ❌ 悪い例: 一度に大量変更
# - 10個のファイルを同時に移動
# - 複数のディレクトリを作成
# - まとめてコミット

# ✅ 良い例: 1つずつ段階的に
# 1. useQuizSettingsフックを抽出
mkdir -p src/hooks
# ... ファイル作成 ...
npm run build  # ビルド確認
git add src/hooks/useQuizSettings.ts
git commit -m "refactor: extract useQuizSettings hook"

# 2. App.tsxで使用
# ... 変更 ...
npm run build  # ビルド確認
git commit -m "refactor: integrate useQuizSettings in App.tsx"

# 3. チェックポイント作成
./scripts/refactor-checkpoint.sh save phase2-quiz-settings-done
```

#### ステップ3: 各変更後の検証
```bash
# 必須チェック
npm run build          # ビルド成功
npm run lint          # Lintパス
npm test              # テストパス (あれば)

# 推奨チェック
npm run dev           # 開発サーバー起動確認
# ブラウザで手動動作確認

# チェックポイント比較
./scripts/refactor-checkpoint.sh compare
```

#### ステップ4: コミット
```bash
# pre-commit-refactorフックが自動実行される
# - src/ファイル数チェック
# - 削除ファイル数チェック
# - ビルドチェック

git add .
git commit -m "refactor(phase2): extract useQuizSettings hook

- Create src/hooks/useQuizSettings.ts (22 lines)
- Extract auto-advance settings logic from App.tsx
- Reduce App.tsx by 15 lines
- All tests passing"

# コミット成功後にチェックポイント作成
./scripts/refactor-checkpoint.sh save phase2-step1-complete
```

#### ステップ5: 定期的なバックアップ
```bash
# 大きな変更の前に必ずタグ作成
git tag -a phase2-before-component-split -m "Phase 2: Before component splitting"

# リモートにプッシュ (pre-pushフックで最終チェック)
git push origin main
git push origin --tags
```

---

## 📊 チェックポイントシステム

### 使い方

#### 1. リファクタリング開始時
```bash
./scripts/refactor-checkpoint.sh save phase2-start
```

#### 2. 各マイルストーン到達時
```bash
# フック抽出完了
./scripts/refactor-checkpoint.sh save phase2-hooks-done

# コンポーネント分割完了
./scripts/refactor-checkpoint.sh save phase2-components-done
```

#### 3. 進捗確認
```bash
./scripts/refactor-checkpoint.sh list
```

出力例:
```
📋 Available checkpoints:
  [20251211-140530] phase2-start
    Files: 116 | Build: success
  [20251211-143015] phase2-hooks-done
    Files: 121 | Build: success
  [20251211-145622] phase2-components-done
    Files: 118 | Build: success
```

#### 4. 問題発生時の復元
```bash
# 現在の状態と比較
./scripts/refactor-checkpoint.sh compare

# チェックポイントに戻る
./scripts/refactor-checkpoint.sh restore phase2-hooks-done
```

### チェックポイントの保管場所

- **JSONファイル**: `.git/refactoring-checkpoints/*.json`
- **Gitタグ**: `checkpoint/phase2-*-YYYYMMDD-HHMMSS`
- **バックアップログ**: 自動記録・永続化

---

## 🔧 トラブルシューティング

### 問題1: Pre-commit-refactorフックが失敗

```bash
❌ ERROR: Too many files deleted: 8
   This might indicate accidental file loss.
```

**原因**: 一度に5ファイル以上削除している

**対処**:
1. 削除ファイル一覧を確認: `git status`
1. 意図的な削除なら以下を実行:
   ```bash
   # pre-commitフックのみスキップ(非推奨)
   SKIP=pre-commit-refactor git commit -m "..."
   
   # またはフックを一時的に無効化
   chmod -x .git/hooks/pre-commit-refactor
   git commit -m "..."
   chmod +x .git/hooks/pre-commit-refactor
   ```
1. **推奨**: 削除を複数のコミットに分割

### 問題2: ビルドが失敗

```bash
❌ ERROR: Build failed after changes
   Run 'npm run build' to see detailed errors
```

**よくある原因**:

1. **インポートパスの更新漏れ**
   ```typescript
   // ❌ 旧パス
   import { useQuizSettings } from '../hooks/useQuizSettings'
   
   // ✅ 新パス
   import { useQuizSettings } from '@/hooks/useQuizSettings'
   ```

1. **型定義の未エクスポート**
   ```typescript
   // hooks/index.ts に追加
   export * from './useQuizSettings'
   ```

1. **循環依存**
   ```typescript
   // A.ts imports B.ts
   // B.ts imports A.ts ← 循環
   ```

**対処**:
```bash
# 詳細エラーを確認
npm run build

# 型チェックのみ実行
npm run typecheck

# エラー修正後、再ビルド
npm run build
```

### 問題3: ファイル数が想定より減少

```bash
⚠️  WARNING: Only 95 files in src/ (baseline: 100+)
```

**確認手順**:
```bash
# 削除されたファイルを確認
git diff --cached --diff-filter=D --name-only | grep "^src/"

# 追加されたファイルを確認
git diff --cached --diff-filter=A --name-only | grep "^src/"

# 変更前の状態に戻す(必要なら)
git reset HEAD
```

**判断基準**:
- **許容**: 統合により2-3ファイル減少 (例: 3つのユーティリティ → 1つに統合)
- **要確認**: 5ファイル以上の減少
- **危険**: 10ファイル以上の減少 (事故の可能性)

---

## 📋 Phase 2リファクタリング チェックリスト

各ステップ実施前に確認:

### 事前準備
- [ ] `./scripts/refactor-checkpoint.sh save <phase-name>` 実行済み
- [ ] `git status` でクリーンな状態を確認
- [ ] `npm run build` が成功することを確認
- [ ] REFACTORING_PLAN.mdで手順を確認済み

### ファイル移動時
- [ ] 移動先ディレクトリを作成
- [ ] ファイルを移動
- [ ] インポートパスを更新(エディタの自動リネーム機能推奨)
- [ ] `npm run build` でビルド成功を確認
- [ ] Git add & commit (小さく分割)
- [ ] チェックポイント作成

### フック抽出時
- [ ] 新しいフックファイル作成
- [ ] ロジックを抽出
- [ ] 型定義を追加
- [ ] JSDocコメント追加
- [ ] `hooks/index.ts` に export 追加
- [ ] 元のコンポーネントで使用
- [ ] `npm run build` で確認
- [ ] コミット

### コンポーネント分割時
- [ ] 新しいコンポーネントファイル作成
- [ ] Propsインターフェース定義
- [ ] ロジックを移動
- [ ] `components/index.ts` に export 追加
- [ ] 親コンポーネントで使用
- [ ] `npm run build` で確認
- [ ] コミット

### 完了確認
- [ ] `npm run build` 成功
- [ ] `npm run lint` パス
- [ ] `npm test` パス (あれば)
- [ ] 開発サーバーで動作確認
- [ ] `./scripts/refactor-checkpoint.sh compare` で変更量確認
- [ ] チェックポイント作成
- [ ] Git push

---

## 🎯 Phase 2リファクタリング計画との統合

`docs/development/REFACTORING_PLAN.md` の Phase 2 を実施する際:

### 推奨される進め方

```bash
# ========== Phase 2 開始 ==========
./scripts/refactor-checkpoint.sh save phase2-start
git tag phase2-start

# ステップ1: useQuizSettings抽出
# ... 実装 ...
npm run build && git commit -m "refactor: extract useQuizSettings"
./scripts/refactor-checkpoint.sh save phase2-step1

# ステップ2: useQuizFilters抽出
# ... 実装 ...
npm run build && git commit -m "refactor: extract useQuizFilters"
./scripts/refactor-checkpoint.sh save phase2-step2

# ステップ3: useQuizState抽出
# ... 実装 ...
npm run build && git commit -m "refactor: extract useQuizState"
./scripts/refactor-checkpoint.sh save phase2-step3

# 以下同様に続ける...

# ========== Phase 2 完了 ==========
./scripts/refactor-checkpoint.sh compare
git tag phase2-complete
git push origin main --tags
```

### 各ステップの目安時間

| ステップ | 作業 | 目安時間 |
|---------|------|---------|
| フック抽出 | 1ファイル | 15-30分 |
| コンポーネント分割 | 1コンポーネント | 30-60分 |
| 統合・テスト | ビルド・動作確認 | 10-15分 |
| チェックポイント | 保存・比較 | 5分 |

**合計**: Phase 2全体で 5-8時間 (実装 + テスト + 安全確認含む)

---

## 📞 緊急時の連絡先・参考資料

### 関連ドキュメント

- [REFACTORING_PLAN.md](./REFACTORING_PLAN.md) - Phase 1-3の詳細計画
- [EMERGENCY_RECOVERY.md](../EMERGENCY_RECOVERY.md) - 緊急復旧ガイド
- [Git Best Practices](https://git-scm.com/book/en/v2)

### フックファイルの場所

```
.git/hooks/
├── pre-commit              # 既存: 型チェック、Lint、ビルド
├── pre-commit-refactor     # 新規: リファクタリング専用チェック
├── pre-rebase              # 自動バックアップ
├── post-rebase             # 整合性チェック
└── pre-push                # 最終安全チェック
```

### チェックポイントデータ

```
.git/
├── refactoring-checkpoints/
│   ├── phase2-start-20251211-140530.json
│   ├── phase2-step1-20251211-141022.json
│   └── ...
└── REBASE_BACKUPS.log
```

---

## ✅ 最終チェックリスト

リファクタリング開始前に確認:

- [ ] 全てのGitフックが実行可能 (`ls -la .git/hooks/`)
- [ ] `scripts/refactor-checkpoint.sh` が実行可能
- [ ] Phase 2の計画を読んで理解した
- [ ] 現在のコードがビルド・テスト成功
- [ ] 初回チェックポイントを作成済み
- [ ] Git作業ブランチをクリーンにした (`git status`)
- [ ] 最新のリモートブランチと同期 (`git pull`)

---

**最後に**: リファクタリングは焦らず、小さく、確実に。1つのステップごとにビルド・コミット・チェックポイントを徹底してください。
