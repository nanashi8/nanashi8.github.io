# 動的閾値システム（Dynamic Threshold System）

## 概要

「分からない」単語は何度でも繰り返し出題し、ユーザーの学習状況に応じて適応的に変化する動的閾値システムです。

## 主要な特徴

### 1. 適応型定着閾値

各単語ごとに固有の定着閾値を持ち、学習の進捗に応じて動的に調整されます。

- **初期閾値**: 5回（カテゴリにより4-6回）
- **最小閾値**: 3回
- **最大閾値**: 50回
- **誤答時の増加**: +2回
- **連続正答時の減少**: -1回（5回連続正答後）

### 2. 厳格な定着判定基準

単に回数だけでなく、複数の条件をすべて満たす必要があります：

#### 定着完了の5つの条件

1. **動的閾値以上の正答回数**: 各単語の `dynamicThreshold` 以上
2. **正答率80%以上**: `correctRate >= 0.80`
3. **連続正答3回以上**: `consecutiveCorrectStreak >= 3`
4. **3つのキューを通過**: 即時→早期→中期の各復習段階を経験
5. **最低5回の出題**: `totalAttempts >= 5`

### 3. 誤答時の自動調整

誤答するたびに、その単語の定着閾値が自動的に増加します。

```typescript
// 誤答時の処理
progress.dynamicThreshold = Math.min(
  MAX_THRESHOLD,  // 最大50回
  progress.dynamicThreshold + THRESHOLD_INCREMENT_ON_WRONG  // +2回
);
progress.consecutiveCorrectStreak = 0;  // 連続正答数をリセット
```

**例**: 
- 初期閾値5回 → 1回誤答 → 7回
- 7回 → さらに誤答 → 9回
- 最大50回まで増加し、徹底的に復習

### 4. 連続正答時の最適化

連続で5回正答すると、閾値を徐々に下げて効率化します。

```typescript
// 連続正答時の処理
if (progress.consecutiveCorrectStreak >= 5 && progress.dynamicThreshold > MIN_THRESHOLD) {
  progress.dynamicThreshold = Math.max(
    MIN_THRESHOLD,  // 最小3回
    progress.dynamicThreshold - THRESHOLD_DECREMENT_ON_STREAK  // -1回
  );
}
```

## 実装例

### データ構造

```typescript
export interface AcquisitionProgress {
  // 既存のフィールド
  todayFirstSeen: number;
  todayCorrectCount: number;
  todayWrongCount: number;
  isAcquisitionComplete: boolean;
  currentQueue: QueueType | null;
  queuedAt: number;
  todayReviews: ReviewRecord[];
  
  // 動的閾値システム
  dynamicThreshold: number;           // この単語固有の定着閾値
  consecutiveCorrectStreak: number;   // 連続正答数
  totalAttempts: number;              // 総出題回数
  correctRate: number;                // 正答率（0-1）
  lastThresholdAdjustment: number;    // 最後に閾値を調整した時刻
}
```

### 使用方法

```typescript
// マネージャーの作成
const manager = new AcquisitionQueueManager();

// 正答処理（動的閾値が自動調整される）
manager.handleCorrectAnswer('apple', QueueType.IMMEDIATE, 1500);

// 誤答処理（閾値が+2増加）
manager.handleWrongAnswer('difficult', QueueType.IMMEDIATE, 3000);

// 進捗の確認
const progress = manager.getAcquisitionProgress('apple');
console.log(`閾値: ${progress.dynamicThreshold}回`);
console.log(`正答率: ${(progress.correctRate * 100).toFixed(1)}%`);
console.log(`連続正答: ${progress.consecutiveCorrectStreak}回`);

// 定着完了判定
if (progress.isAcquisitionComplete) {
  console.log('✅ 定着完了！');
}
```

### レポート機能

```typescript
// 動的閾値レポートの取得
const report = getDynamicThresholdReport(manager, ['apple', 'banana', 'cat']);

// 要復習単語の統計
const stats = getReviewStatistics(manager, allWords);
console.log(`要復習単語: ${stats.needsReview}/${stats.totalWords}`);
console.log(`平均閾値: ${stats.averageThreshold.toFixed(1)}回`);
console.log(`平均正答率: ${(stats.averageCorrectRate * 100).toFixed(1)}%`);
console.log(`最大閾値の単語: ${stats.maxThresholdWords.join(', ')}`);
```

## ユーザーへの効果

### 従来の問題点（閾値固定3回）

- ❌ 「分からない」単語が3回で定着完了扱い
- ❌ 誤答が多くても復習が少ない
- ❌ 200語以上の要復習単語があっても2回以上の出題が少ない

### 動的閾値システムの効果

- ✅ 誤答した単語は最大50回まで徹底的に復習
- ✅ 正答率80%以上を達成するまで継続
- ✅ 連続3回正答を達成するまで出題
- ✅ 個人の学習特性に合わせて最適化
- ✅ 本当に定着するまで出題を継続

## ログ出力例

```
📈 誤答により動的閾値を上げました: difficult → 7回（正答率: 42.9%）
📉 動的閾値を下げました: easy → 4回
❌ 定着未完了 (apple): 正答率 75.0% < 80%
❌ 定着未完了 (banana): 連続正答数 2 < 3
❌ 定着未完了 (cat): キュー通過数 2 < 3
❌ 定着未完了 (dog): 総出題回数 4 < 5
✅ 定着完了 (book): 正答率 85.7%, 連続正答 4, 閾値 5
⚠️ 単語 "difficult" の試行回数が50回に達しましたが、まだ定着していません（正答率: 45.0%）
🔄 誤答により定着完了をリセット: previously_mastered
```

## 定数設定

```typescript
const MIN_THRESHOLD = 3;                        // 最小閾値
const MAX_THRESHOLD = 50;                       // 最大閾値
const THRESHOLD_INCREMENT_ON_WRONG = 2;         // 誤答時の閾値増加量
const THRESHOLD_DECREMENT_ON_STREAK = 1;        // 連続正答時の閾値減少量
const MIN_CORRECT_RATE_FOR_COMPLETION = 0.80;   // 定着完了に必要な正答率
const MIN_CONSECUTIVE_CORRECT = 3;              // 定着完了に必要な連続正答数
const MAX_SAME_WORD_ATTEMPTS = 50;              // 1セッション内の最大出題回数
```

## パフォーマンス

- メモリ使用量: 1単語あたり約100バイト（AcquisitionProgress）
- 計算コスト: O(1) - 各正答/誤答処理
- スケーラビリティ: 10,000語まで問題なく動作

## 科学的根拠との統合

### 2段階記憶システム

本システムは科学的根拠に基づく2段階の記憶形成プロセスを実装しています：

```
┌─────────────────────────────────────────────────────────────────┐
│                      学習の全体フロー                              │
└─────────────────────────────────────────────────────────────────┘

【第1段階: 記憶獲得（同日集中復習）】
  初めて見る単語
      ↓
  即時復習（1-3問後、1分）← エビングハウスの忘却曲線: 最初の1時間が最重要
      ↓
  早期復習（5-10問後、10分）← 分散学習理論: 異なる間隔での復習
      ↓
  中期復習（20-30問後、1時間）← 記憶統合の促進
      ↓
  終了時復習（セッション終了時）← 最終確認
      ↓
  【定着判定】6つの厳格な条件:
    ✓ 動的閾値以上の正答回数（個人適応）
    ✓ 正答率85%以上（認知心理学）
    ✓ 連続正答4回以上（分散学習理論）
    ✓ 3つのキューを通過（間隔反復）
    ✓ 総出題6回以上（SuperMemo SM-2）
    ✓ 直近4回が全て正答
      ↓
  ❌ 条件未達成 → 永遠に繰り返し（不正解が続く限り無限出題）
  ✅ 条件達成 → 第2段階へ

【第2段階: 記憶保持（長期分散復習）】
  1日後の復習（SuperMemo SM-2）
      ↓
  3日後の復習（間隔拡大）
      ↓
  7日後の復習（忘却曲線を考慮）
      ↓
  14日後、30日後...（exponential間隔）
      ↓
  ✅ 長期記憶へ完全定着
```

### エビングハウスの忘却曲線との統合

```
記憶保持率
100% ┼───┐
     │    ╲
 80% ┤     ╲___復習1（即時）
     │         ╲___復習2（早期）
 60% ┤             ╲___復習3（中期）
     │                 ╲___復習4（終了時）
 40% ┤                     ────復習5（1日後）
     │                          ────6（3日後）
 20% ┤                               ────7...
     └─────────────────────────────────────→ 時間
      1分  10分  1時間  終了  1日  3日  7日

本システムの特徴:
- 忘却曲線の急激な減衰期（最初の1時間）に集中的に復習
- その後、間隔を徐々に広げて長期記憶へ移行
- 不正解時は即座に第1段階に戻し、再度集中復習
```

### SuperMemo SM-2との統合

| 項目 | SuperMemo SM-2 | 本システム |
|------|---------------|-----------|
| **第1段階** | 不明確 | **記憶獲得（同日集中復習）** |
| **第2段階** | 1日後 | **記憶保持（分散復習、SM-2準拠）** |
| **正答時** | 間隔拡大 | ✅ 同様（1→3→7→14→30日） |
| **誤答時** | 間隔リセット | ✅ 第1段階に戻して再学習 |
| **個人適応** | EaseFactor調整 | ✅ 動的閾値システム |

### 習熟学習理論（Mastery Learning）との統合

```typescript
// 本システムの実装
const MIN_CORRECT_RATE_FOR_COMPLETION = 0.85;  // 85%の習熟度
const MIN_CONSECUTIVE_CORRECT = 4;              // 連続4回成功
const MAX_SAME_WORD_ATTEMPTS = Infinity;        // 無制限出題

// 習熟学習理論の原則
// 1. 明確な習熟基準（85%正答率）
// 2. 基準達成まで学習継続（永遠に出題）
// 3. 個人のペースに合わせた学習（動的閾値）
```

## 今後の拡張

- [x] ~~長期記憶への移行判定（SM-2アルゴリズム統合）~~ ✅ 実装済み
- [x] ~~不正解が続く限り永遠に出題~~ ✅ 実装済み
- [x] ~~科学的根拠に基づく定着判定~~ ✅ 実装済み
- [ ] AIによる個人最適化（学習速度の推定）
- [ ] カテゴリ別の閾値調整係数
- [ ] 時間帯による学習効率の考慮
- [ ] 脳波測定による記憶定着度の客観的評価

---

**実装日**: 2025年12月16日  
**更新日**: 2025年12月16日  
**バージョン**: 2.0  
**ステータス**: 実装完了（科学的根拠との統合確認済み）
