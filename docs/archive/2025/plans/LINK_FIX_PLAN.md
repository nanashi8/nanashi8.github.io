---
title: 断線リンク修正計画
created: 2025-12-21
updated: 2025-12-21
status: planned
tags: [plan, maintenance, documentation]
---

# 断線リンク修正計画

**現状**: 258個の断線リンクが検出されています
**優先度**: 中（Front Matter作業完了後に実施）
**推定工数**: 3-5時間

## 📊 断線リンク分類

### 1. `.aitk/instructions`への参照 (推定100-150箇所)

**パターン**: `[説明](../.aitk/instructions/~~xxx.instructions.md（ファイル未作成）~~)`  
**原因**: docs内からプロジェクトルートの`.aitk`を相対パスで参照  
**修正方法**:

```bash
# パターン1: 相対パス修正
.aitk/instructions/xxx.md → ../.aitk/instructions/xxx.md

# パターン2: 絶対パス化（推奨）
.aitk/instructions/xxx.md → /.aitk/instructions/xxx.md
```

**修正スクリプト**: `scripts/fix-aitk-links.sh`

```bash
#!/bin/bash
find docs -name "*.md" -exec sed -i '' 's|](../.aitk/|](../.aitk/|g' {} \;
```

### 2. 存在しないファイルへのリンク (推定50-80箇所)

**パターン**:

- `./INTEGRATED_QUALITY_PIPELINE.md` (docs/直下に存在しない)
- `./PHASE_1_TASKS.md` (削除済み)
- `./PLATFORM_ROADMAP.md` (移動済み)

**修正方法**:

- **Option A**: 該当ファイルを作成（内容が必要な場合）
- **Option B**: リンクを削除（obsoleteな場合）
- **Option C**: 正しいパスに修正（移動済みの場合）

**要調査**: 各ファイルの存在意義を確認

### 3. 相対パス解決エラー (推定30-50箇所)

**パターン**: `../specifications/xxx.md` → 正しくは `./specifications/xxx.md`  
**原因**: ディレクトリ構造の誤認識  
**修正方法**: パス検証スクリプトで自動修正

## 📋 修正フェーズ

### Phase 1: 自動修正可能な断線 (2時間)

- `.aitk/instructions`パスの一括置換
- 単純な相対パス誤りの修正
- 重複リンクの除去

**実行コマンド**:

```bash
# 1. .aitkパス修正
./scripts/fix-aitk-links.sh

# 2. 検証
node scripts/analyze-doc-links.mjs

# 3. コミット
git add docs && git commit -m "fix: docs内の.aitkリンクパス修正"
```

### Phase 2: 手動判断が必要な断線 (2時間)

- 存在しないファイルへのリンク処理
  - 各ファイルの必要性を判断
  - 作成 or 削除 or リダイレクト
- archive内の断線
  - 廃止予定ファイルは放置もあり

**判断基準**:

- 参照回数5回以上 → ファイル作成必須
- 参照回数3-4回 → 内容確認後判断
- 参照回数1-2回 → リンク削除検討

### Phase 3: 検証とドキュメント化 (1時間)

- 全リンク再検証
- 修正履歴の記録
- 今後の断線防止策の策定

## 🛡️ 断線防止策

### 1. Pre-commitフックの追加

```bash
# .husky/pre-commit に追加
echo "🔗 リンクチェック実行中..."
node scripts/analyze-doc-links.mjs --quiet || echo "⚠️  断線リンクあり"
```

### 2. CI/CDでの定期チェック

```yaml
# .github/workflows/link-check.yml
name: Documentation Link Check
on:
  push:
    paths:
      - 'docs/**/*.md'
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/analyze-doc-links.mjs
```

### 3. ドキュメント命名規則

- **移動厳禁ファイル**: Front Matterに`doNotMove: true`を追加
- **重要ファイル**: 10回以上参照されるファイルのリスト管理
- **廃止手順**: リンク削除 → 1週間後にファイル削除

## 📅 実施スケジュール

| Phase   | 作業内容     | 所要時間 | 実施予定       |
| ------- | ------------ | -------- | -------------- |
| Phase 1 | 自動修正     | 2時間    | 次回セッション |
| Phase 2 | 手動判断     | 2時間    | Phase 1完了後  |
| Phase 3 | 検証・防止策 | 1時間    | Phase 2完了後  |

**総工数**: 約5時間  
**推奨分割**: 3セッション（各1.5-2時間）

## ⚠️ 注意事項

### 配線保全の鉄則

1. **ファイル名変更禁止**: 特に10回以上参照されるファイル
2. **移動前にリンク更新**: 移動→リンク修正の順は厳禁
3. **削除は段階的**: リンク削除 → 警告期間 → ファイル削除
4. **archive内は慎重**: 過去資料でも参照されている可能性

### 重要ファイル（10回以上参照）

以下のファイルは**絶対に移動・削除禁止**:

- [../specifications/QUESTION_SCHEDULER_SPEC.md](../specifications/QUESTION_SCHEDULER_SPEC.md) - 16回
- [../guidelines/META_AI_TROUBLESHOOTING.md](../guidelines/META_AI_TROUBLESHOOTING.md) - 12回
- [../references/NEW_HORIZON_OFFICIAL_UNIT_STRUCTURE.md](../references/NEW_HORIZON_OFFICIAL_UNIT_STRUCTURE.md) - 10回
- [../specifications/15-data-structures.md](../specifications/15-data-structures.md) - 10回

## 🔍 検証コマンド

```bash
# 断線数確認
node scripts/analyze-doc-links.mjs 2>/dev/null | grep '断線リンク:'

# 断線詳細（上位20件）
node scripts/analyze-doc-links.mjs 2>&1 | head -40

# 特定パターンの検索
grep -r '\.aitk/instructions' docs --include="*.md" | wc -l
```

---

**次回アクション**: Phase 1の自動修正から開始
