# サーバント管轄範囲 - 完全版

## 🎯 サーバントが管理する全ドキュメント

適応的ガードシステムのサーバント（自動化スクリプト）は、**プロジェクト全体のドキュメント**を自動管理できます。

---

## 📊 管轄マップ

```
プロジェクトドキュメント階層
│
├─ 🧠 Instructions（AI教育層）
│  ├─ adaptive-guard-system.instructions.md ✅ 自動生成
│  ├─ ai-code-quality-checklist.instructions.md ✅ 手動作成
│  ├─ refactoring-safety-guide.instructions.md ✅ 手動作成
│  └─ property-naming-convention.instructions.md ✅ 手動作成
│
├─ 📐 Specifications（仕様層）
│  ├─ ADAPTIVE_SPECIFICATIONS.md 🔄 自動生成（新規）
│  ├─ QUESTION_SCHEDULER_SPEC.md ✅ 手動作成
│  └─ TEST_SPECIFICATIONS.md ✅ 手動作成
│
├─ 📋 Guidelines（ガイドライン層）
│  ├─ ADAPTIVE_GUIDELINES.md 🔄 自動生成（新規）
│  ├─ META_AI_INTEGRATION_GUIDE.md ✅ 手動作成
│  └─ CSS_DEVELOPMENT_GUIDELINES.md ✅ 手動作成
│
├─ 🔧 Pipelines（パイプライン層）
│  ├─ adaptive-quality-gate.yml 🔄 自動生成（新規）
│  ├─ adaptive-guard-learning.yml ✅ 手動作成
│  └─ quality-check.yml ✅ 手動作成
│
└─ 📚 Documentation（ドキュメント層）
   ├─ README.md 🔄 自動更新可能
   ├─ FAQ.md 🔄 自動生成可能
   └─ TROUBLESHOOTING.md 🔄 自動生成可能
```

**凡例**:
- ✅ 手動作成（現状維持）
- 🔄 自動生成/更新（新規実装）

---

## 🚀 実装済み機能

### 1. Instructions自動生成 ✅

**スクリプト**: [update-instructions.mjs](../scripts/update-instructions.mjs)

**自動生成ファイル**:
- `.aitk/instructions/adaptive-guard-system.instructions.md`
- `scripts/adaptive-guard-checks.sh`

**トリガー**:
```bash
# 手動実行
npm run update-instructions

# 自動実行（GitHub Actions）
- テスト失敗時
- PRマージ時
```

**生成内容**:
- 高リスク失敗パターン一覧
- パターン別予防策
- 必須チェックリスト
- 収斂状態レポート

---

## 🔄 新規実装機能

### 2. 仕様書自動更新 🆕

**スクリプト**: [update-specifications.mjs](../scripts/update-specifications.mjs)

**自動生成ファイル**:
- `docs/specifications/ADAPTIVE_SPECIFICATIONS.md`

**検出ロジック**:
```javascript
// 高リスクで未文書化のパターンを検出
if (pattern.weight > 0.7 && !pattern.specificationReference) {
  // 仕様の「抜け」として検出
  gaps.push({
    patternId: key,
    suggestedSpecSection: generateSpecSection(pattern)
  });
}
```

**自動生成される仕様セクション**:
```markdown
## property-naming-errorの防止

**優先度**: MUST（必須）
**カテゴリー**: type-error
**リスクレベル**: 1.00

### 問題の説明
型定義を確認せずにプロパティ名を推測

### 予防策
#### 静的解析による自動検出
npm run type-check

### 実装例
#### 誤った実装例
❌ progress.correctCount

#### 正しい実装例
✅ progress.memorizationCorrect

### チェックリスト
- [ ] 型定義を確認
- [ ] npm run type-check を実行
```

**使用方法**:
```bash
# 仕様書自動更新
npm run update-specifications

# 生成された仕様を既存仕様書に反映（手動）
# ADAPTIVE_SPECIFICATIONS.md を確認
# → MUST要件を該当仕様書に追記
```

---

### 3. パイプライン自動更新 🆕

**スクリプト**: [update-pipelines.mjs](../scripts/update-pipelines.mjs)

**自動生成ファイル**:
- `.github/workflows/adaptive-quality-gate.yml`

**検出ロジック**:
```javascript
// 高リスクで静的解析可能なパターンを検出
if (pattern.weight > 0.7 && pattern.prevention.checkType === 'static-analysis') {
  // 新しいCIチェックとして追加
  checks.push({
    name: pattern.id,
    command: pattern.prevention.command,
    errorPattern: pattern.detectionPattern.errorMessage,
    weight: pattern.weight
  });
}
```

**自動生成されるワークフロー**:
```yaml
name: 適応的品質ゲート

jobs:
  adaptive-quality-gate:
    steps:
      - name: 1. property-naming-error (重み: 1.00)
        run: |
          npm run type-check || {
            echo "::error::property-naming-errorエラーが検出されました"
            exit 1
          }
          
      - name: 失敗時の自動更新
        if: failure()
        run: |
          node scripts/update-instructions.mjs all
          node scripts/update-specifications.mjs
          node scripts/update-pipelines.mjs
          git commit -m "🤖 適応的システム: パイプライン自動更新"
```

**使用方法**:
```bash
# パイプライン自動更新
npm run update-pipelines

# GitHub Actionsが自動実行
# → 新しいCIチェックが追加される
# → 失敗時に自己更新
```

---

### 4. 一括更新 🆕

**コマンド**: `npm run update-all-docs`

**実行内容**:
1. Instructions自動更新
2. 仕様書自動更新
3. パイプライン自動更新

**使用シーン**:
- 毎週金曜日（定期メンテナンス）
- 大きな失敗発生後
- リリース前の品質確認

---

## 🤖 自動更新の仕組み

### トリガー1: GitHub Actions（自動）

```yaml
# .github/workflows/adaptive-guard-learning.yml
- name: 失敗時の自動更新
  if: failure()
  run: |
    npm run update-all-docs
    git commit -m "🤖 適応的システム: ドキュメント自動更新"
    git push
```

**動作**:
1. テストが失敗
2. 失敗パターンをデータベースに記録
3. Instructions/仕様書/パイプラインを自動更新
4. Git自動コミット・プッシュ

### トリガー2: 手動実行

```bash
# ローカルで実行
npm run update-all-docs

# 生成されたファイルを確認
git diff

# コミット
git add .
git commit -m "📝 適応的システム: ドキュメント更新"
git push
```

---

## 📊 サーバントの成長モデル

```
失敗発生
  ↓
データベースに記録（failure-patterns.json）
  ↓
┌─────────────────────────────────────┐
│   サーバントが3層を自動更新          │
│                                     │
│ 1. Instructions（AI教育）            │
│    - 適応的Instructions自動生成      │
│                                     │
│ 2. Specifications（仕様）           │
│    - 仕様の「抜け」を検出            │
│    - 新しい仕様セクションを自動生成  │
│                                     │
│ 3. Pipelines（CI/CD）               │
│    - 新しいCIチェックを自動追加      │
│    - ワークフローを自己更新          │
└─────────────────────────────────────┘
  ↓
プロジェクト全体が最新状態に維持される
  ↓
次回から同じ失敗を自動的に防止
```

---

## 🎯 サーバントの管轄拡大

### 現在（Phase 1）
- ✅ Instructions自動生成

### Phase 2（実装済み）
- ✅ 仕様書自動更新
- ✅ パイプライン自動更新

### Phase 3（将来）
- 🔄 README.md自動更新
- 🔄 FAQ自動生成
- 🔄 トラブルシューティング自動生成
- 🔄 ベストプラクティス自動抽出

### Phase 4（理想）
- 🔄 全ドキュメントの自動管理
- 🔄 ドキュメント間の整合性自動チェック
- 🔄 古いドキュメントの自動アーカイブ
- 🔄 ドキュメント品質スコア自動計算

---

## 🎓 サーバントの成長指標

| 指標 | 現在 | Phase 2 | Phase 3 | Phase 4 |
|------|------|---------|---------|---------|
| 自動管理ファイル数 | 2 | 4 | 10+ | 全て |
| カバレッジ | Instructions | +仕様書+Pipeline | +README+FAQ | 100% |
| 自動更新頻度 | 失敗時 | 失敗時+定期 | 毎日 | リアルタイム |
| 人間の介入 | 多い | 中程度 | 少ない | ほぼ不要 |

---

## 📚 参考資料

- [適応的ガードシステム全体像](../ADAPTIVE_GUARD_SYSTEM.md)
- [失敗パターンデータベース](../.aitk/failure-patterns.json)
- [Instructions自動更新スクリプト](../scripts/update-instructions.mjs)
- [仕様書自動更新スクリプト](../scripts/update-specifications.mjs)
- [パイプライン自動更新スクリプト](../scripts/update-pipelines.mjs)

---

**バージョン**: 2.0.0  
**作成日**: 2025-12-20  
**最終更新**: 2025-12-20
