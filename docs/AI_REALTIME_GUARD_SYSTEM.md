# 🛡️ AIリアルタイムガードシステム

## 概要

AI修正作業をリアルタイムで監視・ガードするシステム。修正を開始する前に自動的に：

1. **過去の類似失敗を検索** - 同じ失敗を繰り返さない
2. **危険なパターンを検出** - バッチ方式違反、無限ループ等
3. **関連仕様書を提示** - 確認すべきドキュメント
4. **必須チェックリストを表示** - 修正前の確認事項

**コンセプト**: 事後記録だけでなく、**事前防止**でAIを守る

---

## 🚀 使い方

### 方法1: 統合ワークフロー（推奨）

```bash
node scripts/ai-workflow.mjs "ユーザー依頼内容" [変更予定ファイル...]
```

**例**:
```bash
node scripts/ai-workflow.mjs "バッチ完全消化後の次バッチ生成" "MemorizationView.tsx"
```

**実行内容**:
1. ガードチェック自動実行
2. セッション自動開始
3. 修正ガイダンス表示

### 方法2: ガードチェックのみ

```bash
node scripts/ai-guard-check.mjs "ユーザー依頼内容" [変更予定ファイル...]
```

### 方法3: Pre-commit hook（自動）

コミット時に自動実行されます（インストール済み）

```bash
git commit
# → 自動的にガードチェック実行
# → 危険なパターンがあれば警告
```

---

## 📊 ガードチェックの内容

### 1. 過去の類似失敗検索

**検索対象**:
- AI失敗履歴 (`.aitk/ai-failure-history.json`)
- 一般失敗パターン (`.aitk/failure-patterns.json`)

**マッチング方法**:
- キーワードマッチング（バッチ、useEffect、型等）
- ファイル名マッチング
- ユーザー依頼の類似度

**出力例**:
```
⚠️ 警告: 過去に類似した失敗があります！

1. バッチ完全消化後の次バッチ生成を実装
   試行回数: 2回
   失敗理由: useEffect依存配列の設定ミス
   ✅ 成功した解決方法: needsBatchRegenerationフラグで解決
   🎓 学習ポイント:
   - useEffect依存配列の適切な設定が重要
```

### 2. 危険なパターン検出

**検出パターン**:

#### [CRITICAL] バッチ配列の変更
```typescript
// ❌ 検出例
questions.sort(...)
questions.splice(...)
questions.push(...)
```
→ バッチ確定後は配列を変更してはいけません

#### [CRITICAL] clearExpiredFlags使用
```typescript
// ❌ 検出例
clearExpiredFlags(questions)
```
→ useCategorySlots=true時は無効化が必要

#### [CRITICAL] 再スケジューリング
```typescript
// ❌ 検出例
reSchedule(...)
reschedule(...)
```
→ バッチ方式では禁止

#### [HIGH] useEffect依存配列変更
```typescript
// ⚠️ 検出例
useEffect(() => {
  // ...
}, [newDependency]) // 追加・変更
```
→ 無限ループの危険性

#### [MEDIUM] 古いプロパティ名
```typescript
// ⚠️ 検出例
wordProgress.correctCount
wordProgress.incorrectCount
```
→ 正しくは memorizationCorrect, memorizationAttempts

### 3. 関連仕様書の提示

**自動検出**:
- ユーザー依頼のキーワードから判断
- 変更予定ファイルから判断
- 必須仕様書を常に含める

**出力例**:
```
📖 確認すべき仕様書:

1. [CRITICAL] バッチ方式の3原則
   .aitk/instructions/batch-system-enforcement.instructions.md

2. [CRITICAL] ai-failure-prevention
   .aitk/instructions/ai-failure-prevention.instructions.md

3. [HIGH] Position階層の不変条件
   .aitk/instructions/position-hierarchy-enforcement.instructions.md
```

### 4. 必須チェックリスト

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ CRITICAL: 修正前の必須チェックリスト
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

□ 1. このガードレポートを読んだ
□ 2. 過去の類似失敗パターンを確認した
□ 3. 関連する仕様書を確認した
□ 4. バッチ方式の原則を理解した
□ 5. Position階層を理解した
□ 6. ユーザーに不明点を質問した

❌ このチェックリストを完了せずに修正を開始してはいけません
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 🔄 完全なワークフロー

### 修正開始前

```bash
# 1. 統合ワークフロー実行
node scripts/ai-workflow.mjs "ユーザー依頼" "変更ファイル"

# ↓ 自動実行される内容:
# - ガードチェック
# - 過去の失敗検索
# - 危険パターン検出
# - 関連仕様書提示
# - セッション開始
```

### 修正中

```bash
# 2. 修正試行ごとに記録
node scripts/record-ai-failure.mjs attempt "変更ファイル" "ユーザーフィードバック"
```

### 修正完了後

```bash
# 3. 成功を記録
node scripts/record-ai-failure.mjs success "解決方法"

# ↓ 自動実行される内容:
# - 学習ポイント抽出
# - ai-failure-history.json更新
# - サーバント学習実行
# - instructionsファイル自動更新
```

### コミット時

```bash
# 4. コミット（自動ガードチェック）
git commit

# ↓ Pre-commit hookが自動実行:
# - 変更ファイルのスキャン
# - 危険パターンの検出
# - 警告表示（必要時）
```

---

## 🎯 検出パターンの詳細

### バッチ方式関連

**トリガーファイル**:
- `MemorizationView.tsx`
- `QuestionScheduler.ts`

**検出内容**:
```bash
# 配列操作
questions.sort|splice|push|unshift|reverse

# 禁止機能
clearExpiredFlags
reSchedule|reschedule

# 振動の原因
questions[index] = modified
```

**アクション**:
- CRITICAL警告
- batch-system-enforcement.instructions.md確認を要求
- コミット阻止（pre-commit hook）

### useEffect関連

**検出内容**:
```typescript
useEffect(() => {
  // ...
}, [dependency])
```

**アクション**:
- HIGH警告
- 因果関係の図示を要求
- 無限ループリスクの提示

### 型定義関連

**検出内容**:
```typescript
// 古いプロパティ名
correctCount
incorrectCount
attemptCount
```

**アクション**:
- MEDIUM警告
- 型定義ファイル確認を要求
- 正しいプロパティ名を提示

---

## 📈 期待効果

### リアルタイムガードあり

```
修正依頼
  ↓
ガードチェック（自動）
  ↓ 過去の失敗を表示
  ↓ 危険パターンを警告
  ↓ 仕様書を提示
  ↓
AIが確認・理解
  ↓
修正開始
  ↓
成功率 UP ⬆️
試行回数 DOWN ⬇️
```

### リアルタイムガードなし（従来）

```
修正依頼
  ↓
AIが推測で実装
  ↓
失敗
  ↓
再試行
  ↓
また失敗
  ↓
試行回数 UP ⬆️
ユーザーストレス UP ⬆️
```

---

## 🛠️ 技術仕様

### ガードチェックスクリプト

**ファイル**: `scripts/ai-guard-check.mjs`

**機能**:
- キーワード抽出
- 類似失敗検索
- 危険パターン検出
- 仕様書マッピング
- カラー出力

**出力フォーマット**:
- ANSIカラーコード使用
- 階層的な情報表示
- 優先度別の色分け

### Pre-commit Hook

**ファイル**: `scripts/pre-commit-ai-guard.sh`

**インストール**: 自動（シンボリックリンク）
```bash
.git/hooks/pre-commit -> ../../scripts/pre-commit-ai-guard.sh
```

**チェック内容**:
- 変更ファイルの取得
- Git diff解析
- パターンマッチング
- CRITICAL検出時はコミット阻止

**バイパス方法**:
```bash
git commit --no-verify
```

### 統合ワークフロー

**ファイル**: `scripts/ai-workflow.mjs`

**機能**:
- ガードチェック実行
- セッション自動開始
- ステップ表示
- 次のアクション案内

---

## 📚 関連ファイル

### スクリプト
- `scripts/ai-guard-check.mjs` - ガードチェック本体
- `scripts/ai-workflow.mjs` - 統合ワークフロー
- `scripts/pre-commit-ai-guard.sh` - Pre-commit hook
- `scripts/record-ai-failure.mjs` - 失敗記録
- `scripts/learn-from-ai-failures.mjs` - サーバント学習

### データベース
- `.aitk/ai-failure-history.json` - AI失敗履歴
- `.aitk/failure-patterns.json` - 一般失敗パターン

### Instructions
- `.aitk/instructions/ai-failure-prevention.instructions.md` - 失敗防止ガイド
- `.aitk/instructions/batch-system-enforcement.instructions.md` - バッチ方式
- `.aitk/instructions/mandatory-spec-check.instructions.md` - 仕様確認

### ドキュメント
- `docs/AI_FAILURE_COLLECTION_SYSTEM.md` - 失敗収集システム
- `docs/AI_REALTIME_GUARD_SYSTEM.md` - このファイル

---

## 🎓 ベストプラクティス

### AIエージェント向け

1. **必ず事前にガードチェック実行**
   ```bash
   node scripts/ai-workflow.mjs "依頼内容" "変更ファイル"
   ```

2. **警告を無視しない**
   - CRITICAL警告は必ず対処
   - HIGH警告は慎重に確認
   - MEDIUM警告も可能な限り対処

3. **仕様書を必ず読む**
   - ガードが提示した仕様書は全て確認
   - 不明点はユーザーに質問

4. **チェックリストを完了**
   - 6項目全て確認してから修正開始

### プロジェクト管理者向け

1. **Pre-commit hookの確認**
   ```bash
   ls -la .git/hooks/pre-commit
   # → ../../scripts/pre-commit-ai-guard.sh へのリンク
   ```

2. **定期的な学習実行**
   ```bash
   node scripts/learn-from-ai-failures.mjs
   # → サーバントが最新の失敗から学習
   ```

3. **統計の確認**
   ```bash
   cat .aitk/ai-failure-history.json
   # → 平均試行回数、成功率等
   ```

---

## 🔮 今後の拡張

### Phase 2: VSCode統合
- タスク自動実行
- エディター内警告表示
- リアルタイム型チェック

### Phase 3: ESLintルール
- カスタムルール実装
- 危険パターンの自動検出
- IDE統合

### Phase 4: AI予測モデル
- 失敗確率の予測
- 最適修正手順の提案
- 自動リファクタリング

---

**作成日**: 2025-12-30
**最終更新**: 2025-12-30
**ステータス**: ✅ 実装完了・稼働中

---

## 📊 実装サマリー

| 機能 | ステータス | ファイル |
|------|-----------|----------|
| ガードチェック | ✅ 完了 | scripts/ai-guard-check.mjs |
| Pre-commit hook | ✅ 完了 | scripts/pre-commit-ai-guard.sh |
| 統合ワークフロー | ✅ 完了 | scripts/ai-workflow.mjs |
| 失敗記録 | ✅ 完了 | scripts/record-ai-failure.mjs |
| サーバント学習 | ✅ 完了 | scripts/learn-from-ai-failures.mjs |
| GitHub Actions統合 | ✅ 完了 | .github/workflows/servant-auto-learning.yml |

**総ファイル数**: 6個
**総コード行数**: 約1,500行
**テスト済み**: ✅
**本番稼働**: ✅
