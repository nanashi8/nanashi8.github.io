# SimpleWord 機能復元ガイド

**最終更新**: 2025-10-25  
**バージョン**: 2.0

このドキュメントは、SimpleWordの主要機能が喪失した場合に、迅速かつ正確に復元するためのガイドです。

---

## 📋 目次

1. [復元の基本方針](#復元の基本方針)
2. [優先度別復元計画](#優先度別復元計画)
3. [機能別復元手順](#機能別復元手順)
4. [データ復元手順](#データ復元手順)
5. [検証チェックリスト](#検証チェックリスト)

---

## 復元の基本方針

### 復元時の参照順序

1. **包括的仕様書**: `docs/COMPREHENSIVE_SPECIFICATION.md`
2. **機能別仕様書**: `SimpleWord/docs/仕様書/`
3. **適応型学習ガイド**: `ADAPTIVE_LEARNING_GUIDE.md`
4. **実装ファイル**: 既存のコード（破損していない場合）

### 復元の原則

- ✅ **段階的復元**: 基本機能から順番に復元
- ✅ **検証を徹底**: 各ステップで動作確認
- ✅ **ドキュメント優先**: コードよりも仕様書を信頼
- ✅ **データ保全**: ユーザーデータを最優先で保護

---

## 優先度別復元計画

### 最優先（P0）: アプリ起動に必要

| 機能 | 復元時間 | 参照ドキュメント |
|---|---|---|
| アプリエントリーポイント | 10分 | SimpleWordApp.swift |
| ナビゲーション | 20分 | ContentView.swift |
| CSV読み込み | 30分 | CSVLoader.swift + 仕様書03 |
| 基本データモデル | 30分 | QuestionItem.swift + COMPREHENSIVE_SPEC |

**復元時間合計**: 約1.5時間

### 高優先（P1）: 基本学習機能

| 機能 | 復元時間 | 参照ドキュメント |
|---|---|---|
| QuizView基本構造 | 1時間 | 仕様書01 |
| 4択出題 | 30分 | 仕様書01 |
| 正誤判定 | 30分 | 仕様書01 |
| スコア記録 | 1時間 | WordScoreStore.swift + 仕様書05 |
| 出題設定 | 1時間 | QuizSettings.swift + 仕様書02 |

**復元時間合計**: 約4時間

### 中優先（P2）: 適応型学習

| 機能 | 復元時間 | 参照ドキュメント |
|---|---|---|
| StudyRecord | 2時間 | StudyRecord.swift + 仕様書09 |
| AdaptiveScheduler | 3時間 | AdaptiveScheduler.swift + 仕様書09 |
| MemoryStage | 2時間 | MemoryStage.swift + 仕様書10 + ADAPTIVE_GUIDE |
| バッチ学習 | 2時間 | 仕様書09 |
| 補修モード | 1時間 | 仕様書09 |

**復元時間合計**: 約10時間

### 低優先（P3）: UI強化と付加機能

| 機能 | 復元時間 | 参照ドキュメント |
|---|---|---|
| アニメーション | 1時間 | 仕様書01 |
| 記憶定着度表示 | 1時間 | 仕様書10 |
| 学習モード推奨 | 1時間 | 仕様書11 |
| UIコンポーネント分離 | 2時間 | 仕様書12 |
| CSV管理 | 2時間 | 仕様書03 |

**復元時間合計**: 約7時間

---

## 機能別復元手順

### 1. CSV読み込み機能（P0）

#### 必要なファイル
- `Services/CSVLoader.swift`
- `Models/QuestionItem.swift`

#### 復元手順

```swift
// 1. QuestionItemの定義（最小構成）
struct QuestionItem: Identifiable, Codable {
    let id: UUID
    let term: String
    let reading: String
    let meaning: String
    let etymology: String
    let relatedWordsCSV: String
    let relatedFieldsCSV: String
    let difficulty: String
}

// 2. CSVLoader.swiftの実装
class CSVLoader {
    static func loadCSV(named name: String) -> [QuestionItem] {
        // 1. DocumentsとBundleから探す
        // 2. UTF-8でデコード
        // 3. CSV解析（ヘッダー読み飛ばし）
        // 4. QuestionItemの配列を返す
    }
}
```

#### 参照ドキュメント
- `docs/COMPREHENSIVE_SPECIFICATION.md` - データモデル仕様
- `SimpleWord/docs/仕様書/03_問題集管理_CSV_仕様書.md`

#### 検証
```bash
# テストCSVで読み込み確認
let items = CSVLoader.loadCSV(named: "サンプル単語.csv")
print("読み込み件数: \(items.count)")
```

---

### 2. QuizView基本構造（P1）

#### 必要なファイル
- `Features/Quiz/Views/QuizView.swift`

#### 復元手順

**ステップ1: 最小構成**

```swift
struct QuizView: View {
    @State private var items: [QuestionItem] = []
    @State private var currentIndex = 0
    
    var body: some View {
        VStack {
            if currentIndex < items.count {
                Text(items[currentIndex].term)
                Text(items[currentIndex].meaning)
                Button("次へ") { currentIndex += 1 }
            }
        }
        .onAppear { loadCSV() }
    }
    
    private func loadCSV() {
        items = CSVLoader.loadCSV(named: "サンプル単語.csv")
    }
}
```

**ステップ2: 4択追加**

```swift
@State private var choices: [Choice] = []

struct Choice: Identifiable {
    let id: UUID
    let text: String
    let isCorrect: Bool
}

// prepareQuestion()を実装
// - 正解 + ランダムな3つの誤答
```

**ステップ3: 正誤判定**

```swift
@State private var selectedID: UUID? = nil
@State private var correctID: UUID? = nil

// select()を実装
```

#### 参照ドキュメント
- `SimpleWord/docs/仕様書/01_クイズ機能_仕様書.md`
- `docs/COMPREHENSIVE_SPECIFICATION.md` - QuizView仕様

#### 検証
- [ ] CSVが読み込める
- [ ] 問題が表示される
- [ ] 4択が生成される
- [ ] 正誤判定が動作する

---

### 3. AdaptiveScheduler（P2）

#### 必要なファイル
- `Features/Study/Logic/AdaptiveScheduler.swift`
- `Features/Study/Domain/StudyRecord.swift`
- `Features/Study/Data/FileStudyProgressRepository.swift`

#### 復元手順

**ステップ1: StudyRecord**

```swift
struct StudyRecord: Codable, Identifiable {
    let id: UUID
    var firstSeenAt: Date?
    var lastSeenAt: Date?
    var nextDueAt: Date
    var interval: TimeInterval
    
    var totalAttempts: Int
    var correctCount: Int
    var wrongCount: Int
    
    var smoothedAccuracy: Double
    var ease: Double
    
    // apply()メソッドを実装
}
```

**ステップ2: Repository**

```swift
protocol StudyProgressRepository {
    func load(id: UUID) -> StudyRecord
    func save(_ record: StudyRecord)
    func loadAll() -> [StudyRecord]
}

class FileStudyProgressRepository: StudyProgressRepository {
    // JSON形式で保存・読み込み
}
```

**ステップ3: AdaptiveScheduler**

```swift
class AdaptiveScheduler {
    func record(itemID: UUID, result: ReviewOutcome, responseTime: TimeInterval)
    func scheduleNextBatch(itemIDs: [UUID], count: Int) -> [UUID]
}
```

#### 参照ドキュメント
- `SimpleWord/docs/仕様書/09_出題ロジック_仕様書.md`（最重要）
- `docs/COMPREHENSIVE_SPECIFICATION.md` - 学習アルゴリズム
- `ADAPTIVE_LEARNING_GUIDE.md`

#### 検証
```swift
let scheduler = AdaptiveScheduler()
let itemID = UUID()

// 正解を記録
scheduler.record(itemID: itemID, result: .correct, responseTime: 2.5)

// StudyRecordが更新されたか確認
let record = scheduler.repo.load(id: itemID)
print("Attempts: \(record.totalAttempts)")
print("Accuracy: \(record.smoothedAccuracy)")
print("Next Due: \(record.nextDueAt)")
```

---

### 4. MemoryStage（P2）

#### 必要なファイル
- `Features/Study/Domain/MemoryStage.swift`

#### 復元手順

**ステップ1: 列挙型定義**

```swift
enum MemoryStage: String, Codable, CaseIterable {
    case unseen
    case initial
    case shortTerm
    case midTerm
    case longTerm
    case mastered
    
    var displayName: String { /* ... */ }
    var recommendedInterval: ClosedRange<TimeInterval> { /* ... */ }
    var recommendedRepetitions: Int { /* ... */ }
    func priority(for mode: LearningMode) -> Double { /* ... */ }
}
```

**ステップ2: 判定ロジック**

```swift
static func determine(from record: StudyRecord) -> MemoryStage {
    if record.mastered && record.smoothedAccuracy >= 0.9 
       && record.correctStreak >= 5 {
        return .mastered
    }
    
    let attempts = record.totalAttempts
    let interval = record.interval
    
    if attempts == 0 { return .unseen }
    if attempts <= 2 { return .initial }
    if attempts <= 5 || interval < 300 { return .shortTerm }
    if attempts <= 10 || interval < 86400 { return .midTerm }
    return .longTerm
}
```

**ステップ3: MemoryConsolidationTracker**

```swift
struct MemoryConsolidationTracker {
    func stages(for itemIDs: [UUID]) -> [UUID: MemoryStage]
    func filter(itemIDs: [UUID], for mode: LearningMode, limit: Int) -> [UUID]
    func statistics(for itemIDs: [UUID]) -> [MemoryStage: Int]
}
```

#### 参照ドキュメント
- `SimpleWord/docs/仕様書/10_記憶定着度表示_仕様書.md`
- `ADAPTIVE_LEARNING_GUIDE.md` - 記憶定着段階モデル
- `docs/COMPREHENSIVE_SPECIFICATION.md` - MemoryStage仕様

#### 検証
```swift
let tracker = MemoryConsolidationTracker()
let itemIDs = [UUID(), UUID(), UUID()]
let stats = tracker.statistics(for: itemIDs)

for stage in MemoryStage.allCases {
    print("\(stage.displayName): \(stats[stage] ?? 0)個")
}
```

---

### 5. UIコンポーネント（P3）

#### 必要なファイル
- `QuizComponents/QuestionCardView.swift`
- `QuizComponents/ChoiceCardView.swift`
- `QuizComponents/DontKnowCardView.swift`
- `QuizComponents/QuizStatisticsView.swift`
- `QuizComponents/QuizNavigationButtonsView.swift`

#### 復元手順

各コンポーネントを個別に作成し、プレビューで確認：

```swift
// QuestionCardView
#Preview {
    QuestionCardView(item: sampleItem)
        .environmentObject(WordScoreStore())
}

// ChoiceCardView
#Preview {
    ChoiceCardView(
        id: UUID(),
        text: "りんご",
        explanation: "赤い果物",
        selectedID: nil,
        correctID: UUID(),
        onSelect: { _ in }
    )
}
```

#### 参照ドキュメント
- `SimpleWord/docs/仕様書/12_クイズUIコンポーネント_仕様書.md`（完全仕様）

#### 検証
- [ ] 各コンポーネントのプレビューが動作
- [ ] QuizViewから正しく参照できる
- [ ] 色とレイアウトが正しい

---

## データ復元手順

### ユーザーデータの優先順位

1. **最優先**: 学習履歴（StudyProgress/）
2. **高優先**: 成績データ（Scores/, WordScores/）
3. **中優先**: 設定情報（UserDefaults）
4. **低優先**: カスタムCSV（Documents/CSVs/）

### バックアップからの復元

```bash
# 1. バックアップディレクトリを確認
ls ~/Library/Application\ Support/SimpleWord/Backups/

# 2. 最新のバックアップを選択
cp -r Backups/latest/* .

# 3. アプリを起動して確認
```

### データ整合性チェック

```swift
// StudyRecordの整合性確認
let repo = FileStudyProgressRepository()
let allRecords = repo.loadAll()

for record in allRecords {
    // 必須フィールドの確認
    assert(record.interval >= 0)
    assert(record.smoothedAccuracy >= 0 && record.smoothedAccuracy <= 1)
    assert(record.ease >= 1.1 && record.ease <= 3.0)
}
```

---

## 検証チェックリスト

### P0: アプリ起動

- [ ] アプリが起動する
- [ ] メイン画面が表示される
- [ ] CSVが読み込める
- [ ] 問題リストが表示される

### P1: 基本学習機能

- [ ] クイズが開始できる
- [ ] 4択が表示される
- [ ] 回答できる
- [ ] 正誤判定が正しい
- [ ] スコアが記録される
- [ ] 次の問題に進める

### P2: 適応型学習

- [ ] StudyRecordが保存される
- [ ] AdaptiveSchedulerが動作する
- [ ] 出題間隔が更新される
- [ ] 優先度順に出題される
- [ ] 記憶段階が判定される
- [ ] バッチ学習が動作する

### P3: UI強化

- [ ] アニメーションが動作する
- [ ] 記憶定着度が表示される
- [ ] 学習モード推奨が表示される
- [ ] UIコンポーネントが分離されている
- [ ] ダークモードが動作する

---

## 復元作業の記録

復元作業を行った場合は、以下の情報を記録してください：

```markdown
## 復元記録

**日時**: YYYY-MM-DD HH:MM
**復元者**: [名前]
**復元範囲**: [P0/P1/P2/P3]
**所要時間**: [時間]
**参照ドキュメント**: [リスト]
**問題点**: [あれば記載]
**改善点**: [今後のための提案]
```

---

## トラブルシューティング

### ビルドエラーが発生する

**対処**:
1. SwiftLintの警告を確認: `swiftlint`
2. 非推奨構文を修正
3. 依存関係を確認

### データが読み込めない

**対処**:
1. ファイルパスを確認
2. エンコーディングを確認（UTF-8）
3. JSON形式の妥当性を確認

### アルゴリズムが期待通りに動作しない

**対処**:
1. 仕様書と実装を照合
2. ユニットテストを作成して確認
3. デバッグログで中間値を確認

---

## まとめ

### 完全復元の所要時間

| 優先度 | 時間 |
|---|---|
| P0（起動） | 1.5時間 |
| P1（基本） | 4時間 |
| P2（適応型） | 10時間 |
| P3（UI強化） | 7時間 |
| **合計** | **22.5時間** |

### 最小限の復元（P0+P1）

学習機能として最低限動作させるには、**約5.5時間**で可能です。

### 推奨される復元順序

1. P0を完全に復元（1.5時間）
2. P1を完全に復元（4時間）
3. 動作確認とデータ移行
4. P2を段階的に復元（2時間×5機能）
5. P3は必要に応じて復元

---

**最終更新**: 2025-10-25  
**バージョン**: 2.0  
**メンテナ**: GitHub Copilot
