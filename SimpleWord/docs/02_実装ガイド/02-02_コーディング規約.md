# 02-02 コーディング規約

**文書番号**: 02-02  
**最終更新**: 2025-11-04  
**バージョン**: 2.0  
**ステータス**: 最新

---

## 📋 目次

1. [基本方針](#基本方針)
2. [命名規則](#命名規則)
3. [コード構成](#コード構成)
4. [コメント規約](#コメント規約)
5. [アーキテクチャ原則](#アーキテクチャ原則)
6. [Swift固有のルール](#swift固有のルール)
7. [禁止事項](#禁止事項)

---

## 🎯 基本方針

### 1. シンプルさの追求

```swift
// ✅ 良い例：シンプルで読みやすい
func calculateScore(correct: Int, total: Int) -> Double {
    guard total > 0 else { return 0.0 }
    return Double(correct) / Double(total) * 100
}

// ❌ 悪い例：過度に複雑
func calculateScore(correct: Int, total: Int) -> Double {
    return total > 0 ? (correct as NSNumber).doubleValue / (total as NSNumber).doubleValue * 100 : 0.0
}
```

### 2. 可読性の重視

- コードは書く時間より読む時間の方が長い
- 明確な命名を優先
- マジックナンバーを避ける

### 3. 保守性の確保

- 単一責任の原則
- DRY原則（Don't Repeat Yourself）
- YAGNI原則（You Aren't Gonna Need It）

---

## 📝 命名規則

### 型名（クラス、構造体、列挙型、プロトコル）

**規則**: UpperCamelCase（各単語の先頭を大文字）

```swift
// ✅ 良い例
class QuizSessionStore { }
struct QuestionItem { }
enum MemoryStage { }
protocol QuestionDataSource { }

// ❌ 悪い例
class quizSessionStore { }  // 先頭小文字
struct questionitem { }     // キャメルケースでない
```

### 変数・定数・関数名

**規則**: lowerCamelCase（先頭は小文字、それ以降の単語は大文字）

```swift
// ✅ 良い例
let currentIndex: Int
var selectedChoice: Int?
func calculatePriority() -> Double

// ❌ 悪い例
let CurrentIndex: Int       // 先頭大文字
var selected_choice: Int?   // スネークケース
```

### プライベートプロパティ

**規則**: プレフィックスなし（Swiftの標準）

```swift
// ✅ 良い例
private var selectedChoice: Int?
private let dataSource: CSVDataSource

// ❌ 悪い例（Objective-C風）
private var _selectedChoice: Int?
```

### 定数

**規則**: lowerCamelCase（グローバル定数のみ大文字も可）

```swift
// ✅ 良い例
let maxBatchSize = 20
private let defaultDifficulty = 1

// グローバル定数（大文字も許可）
let MAX_DIFFICULTY = 5

// ❌ 悪い例
let MAX_BATCH_SIZE = 20  // ローカル定数で大文字
```

### Bool型の命名

**規則**: is/has/canなどの接頭辞を使用

```swift
// ✅ 良い例
var isCompleted: Bool
var hasError: Bool
var canSubmit: Bool

// ❌ 悪い例
var completed: Bool  // 状態が不明確
var error: Bool      // Bool型であることが不明確
```

### 省略形の使用

**規則**: 一般的な省略形以外は避ける

```swift
// ✅ 許可される省略形
let id: String          // identifier
let url: URL            // Uniform Resource Locator
let csv: String         // Comma-Separated Values
let maxCount: Int       // maximum
let minValue: Int       // minimum

// ❌ 避けるべき省略形
let ques: String        // question と明記
let ans: Int           // answer と明記
let cnt: Int           // count と明記
```

---

## 🏗️ コード構成

### インデント

**規則**: スペース4つ

```swift
// ✅ 良い例
func calculateScore() -> Double {
    guard total > 0 else {
        return 0.0
    }
    return Double(correct) / Double(total)
}

// ❌ 悪い例（タブまたはスペース2つ）
func calculateScore() -> Double {
  guard total > 0 else {
    return 0.0
  }
  return Double(correct) / Double(total)
}
```

### ファイル構成

**推奨順序**:

```swift
// 1. ファイルヘッダコメント
// QuizView.swift
// クイズ画面の実装

import SwiftUI

// 2. 型定義
struct QuizView: View {
    
    // 3. プロパティ（順序：@State, @ObservedObject, private, public）
    @State private var selectedChoice: Int?
    @ObservedObject var settings: QuizSettings
    private let maxCount: Int = 20
    
    // 4. 初期化
    init(settings: QuizSettings) {
        self.settings = settings
    }
    
    // 5. body
    var body: some View {
        // UI実装
    }
    
    // 6. privateメソッド
    private func submitAnswer() {
        // 実装
    }
    
    // 7. ヘルパー・計算プロパティ
    private var isCompleted: Bool {
        // 実装
    }
}

// 8. 拡張（必要に応じて）
extension QuizView {
    // 拡張メソッド
}

// 9. プレビュー
#Preview {
    QuizView(settings: QuizSettings())
}
```

### クラス・構造体の構成

```swift
struct QuestionItem {
    // MARK: - Properties
    let id: String
    let term: String
    
    // MARK: - Initialization
    init(id: String, term: String) {
        self.id = id
        self.term = term
    }
    
    // MARK: - Public Methods
    func validate() -> Bool {
        // 実装
    }
    
    // MARK: - Private Methods
    private func internalValidation() -> Bool {
        // 実装
    }
}
```

---

## 💬 コメント規約

### ファイルヘッダ

**規則**: 日本語で簡潔に目的を記載

```swift
//
// QuizView.swift
// SimpleWord
//
// クイズ画面の実装
// 4択形式でユーザーに問題を出題し、回答を記録する
//
```

### 関数・メソッドのコメント

**規則**: 複雑なロジックには日本語で説明を追加

```swift
/// 適応型学習アルゴリズムに基づいて次のバッチを生成
/// 
/// - Parameters:
///   - allItems: すべての問題アイテム
///   - size: バッチサイズ
/// - Returns: 優先度順にソートされた問題リスト
func scheduleNextBatch(from allItems: [QuestionItem], size: Int) -> [QuestionItem] {
    // 実装
}
```

### インラインコメント

**規則**: 複雑な処理や注意が必要な箇所に日本語で追加

```swift
// ✅ 良い例：意図を明確に説明
// CSV固定列3から選択肢を生成（ヘッダに依存しない）
let correctAnswer = correctItem.rawColumns.count > 2 
    ? correctItem.rawColumns[2] 
    : correctItem.meaning

// ❌ 悪い例：コードの直訳
// rawColumnsのcountが2より大きい場合
let correctAnswer = correctItem.rawColumns.count > 2 
    ? correctItem.rawColumns[2] 
    : correctItem.meaning
```

### TODO・FIXME・MARK

```swift
// TODO: 将来的にはCloudKitで同期機能を実装
// FIXME: iOS 15以下でクラッシュする可能性がある
// MARK: - Private Methods
```

---

## 🏛️ アーキテクチャ原則

### 1. Feature-First Architecture

**機能ごとに垂直分割**:

```
Features/
├── Quiz/          # クイズ機能（自己完結）
│   ├── Views/
│   ├── Logic/
│   └── Models/
└── Study/         # 学習機能（自己完結）
    ├── Domain/
    ├── Logic/
    └── Data/
```

### 2. 単一責任の原則（SRP）

```swift
// ✅ 良い例：責務が明確
class CSVLoader {
    func load(from url: URL) -> [QuestionItem] {
        // CSVの読み込みのみ
    }
}

class QuizSessionStore {
    func startSession(with items: [QuestionItem]) {
        // セッション管理のみ
    }
}

// ❌ 悪い例：責務が混在
class QuizManager {
    func loadAndStartSession(from url: URL) {
        // CSV読み込み + セッション管理 + 統計計算...
    }
}
```

### 3. 依存性の注入

```swift
// ✅ 良い例：依存性を注入
class AdaptiveScheduler {
    private let dataSource: QuestionDataSource
    
    init(dataSource: QuestionDataSource) {
        self.dataSource = dataSource
    }
}

// ❌ 悪い例：直接依存
class AdaptiveScheduler {
    private let dataSource = CSVDataSource()  // 固定依存
}
```

### 4. 過度なラッパーを避ける

```swift
// ✅ 良い例：直接使用
let items = try await CSVDataSource().load(from: url)

// ❌ 悪い例：不要なラッパー
class DataManager {
    func loadData() {
        let loader = DataLoader()
        let items = loader.load()  // 単なるラッパー
    }
}
```

---

## 🚀 Swift固有のルール

### Optional型の扱い

```swift
// ✅ 良い例：guard letで早期リターン
func process(item: QuestionItem?) {
    guard let item = item else { return }
    // itemを使った処理
}

// ✅ 良い例：nil合体演算子
let value = optionalValue ?? defaultValue

// ❌ 悪い例：強制アンラップ
let value = optionalValue!  // クラッシュの危険性
```

### エラー処理

```swift
// ✅ 良い例：具体的なエラー処理
do {
    let items = try csvLoader.load(from: url)
    processItems(items)
} catch CSVError.invalidFormat {
    showAlert("CSVフォーマットが不正です")
} catch {
    showAlert("エラーが発生しました: \(error.localizedDescription)")
}

// ❌ 悪い例：エラーを無視
try? csvLoader.load(from: url)  // エラーを握りつぶす
```

### クロージャ

```swift
// ✅ 良い例：trailing closure
items.filter { $0.difficulty <= maxDifficulty }

// ✅ 良い例：複数行の場合は明示的
items.filter { item in
    item.difficulty <= maxDifficulty &&
    item.relatedFields.contains(targetField)
}

// ❌ 悪い例：読みにくい
items.filter({ $0.difficulty <= maxDifficulty && $0.relatedFields.contains(targetField) && $0.term.count > 0 })
```

### SwiftUI View

```swift
// ✅ 良い例：@ViewBuilderで条件分岐
@ViewBuilder
var contentView: some View {
    if isLoading {
        ProgressView()
    } else {
        QuizContent()
    }
}

// ❌ 悪い例：AnyViewの乱用
var contentView: some View {
    if isLoading {
        return AnyView(ProgressView())
    } else {
        return AnyView(QuizContent())
    }
}
```

---

## 🚫 禁止事項

### 1. 非推奨構文の使用

```swift
// ❌ 禁止：非推奨構文
.overlay(alignment: .top) {
    Text("Deprecated")
}

// ✅ 使用：最新の構文
.overlay {
    Text("Modern")
}
```

詳細は`DEPRECATION_GUIDE.md`を参照。

### 2. マジックナンバー

```swift
// ❌ 悪い例
if difficulty < 3 { }

// ✅ 良い例
let mediumDifficulty = 3
if difficulty < mediumDifficulty { }
```

### 3. 過度なネスト

```swift
// ❌ 悪い例：3段階以上のネスト
if condition1 {
    if condition2 {
        if condition3 {
            // 深すぎる
        }
    }
}

// ✅ 良い例：早期リターンでフラット化
guard condition1 else { return }
guard condition2 else { return }
guard condition3 else { return }
// 処理
```

### 4. グローバル変数の乱用

```swift
// ❌ 悪い例
var globalQuizState = QuizState()

// ✅ 良い例：状態管理クラスで管理
@EnvironmentObject var quizState: QuizState
```

---

## ✅ チェックリスト

新しいコードを書く前に確認：

- [ ] 命名規則に従っているか？
- [ ] インデントはスペース4つか？
- [ ] 日本語コメントで意図を説明しているか？
- [ ] 単一責任の原則を守っているか？
- [ ] 非推奨構文を使用していないか？
- [ ] テストを書いたか？
- [ ] ドキュメントを更新したか？

---

## 📚 関連ドキュメント

- **アーキテクチャ**: `01-02_アーキテクチャ仕様.md`
- **ディレクトリ構造**: `02-03_ディレクトリ構造.md`
- **非推奨構文**: `DEPRECATION_GUIDE.md`（ルート）
- **テスト**: `02-06_テスト戦略.md`

---

**文書履歴**:
- 2025-11-04: 初版作成、グローバルコーディング規約を統合

**管理者**: SimpleWord開発チーム
