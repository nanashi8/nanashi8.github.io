# 学習モード推奨機能_仕様書

**最終更新: 2025-10-25**

この文書は、学習モード別の推奨設定を表示する機能（LearningModeRecommendationView）の復元・実装ガイドです。

## ⚠️ 重要: 復元のための必須情報

この機能が喪失した場合、このドキュメントから完全に復元できます。

---

## 概要

### 何を実現しているか
- 各学習モード（通常・復習・補習）の説明を表示
- モードごとの推奨される使い方を提示
- 適したシーンと学習のコツを明示
- アイコンと色で視覚的に区別

### なぜ必要か
- ユーザーが自分に適した学習モードを選択できる
- 各モードの効果と使い分けを理解できる
- 学習の目的に応じた最適な設定を提案

---

## 学習モードの種類

### LearningMode列挙型

```swift
public enum LearningMode: String, Codable, CaseIterable, Identifiable {
    public var id: String { rawValue }
    case normal = "normal"          // 通常モード
    case review = "review"          // 復習モード
    case remediation = "remediation" // 補習モード

    public var displayName: String {
        switch self {
        case .normal: return "通常"
        case .review: return "復習"
        case .remediation: return "補習"
        }
    }
}
```

---

## モード別の詳細仕様

### 1. 通常モード（Normal）

#### アイコン・色
- **アイコン**: `book.fill`
- **色**: Blue (#007AFF)

#### 説明
> 新しい単語と復習が必要な単語をバランスよく学習します。初見の単語は集中的に反復し、既習単語は適度な間隔で復習します。

#### 推奨シーン
- ✅ 日常的な学習
- ✅ 新しい単語帳を始めるとき
- ✅ 学習ペースを安定させたいとき
- ✅ 幅広く単語を覚えたいとき

#### 学習のコツ
- 💡 毎日コンスタントに学習する
- 💡 新規単語と復習のバランスを保つ
- 💡 焦らず着実に進める

#### 出題の特徴
- 未学習単語: 優先度 5.0
- 初回接触: 優先度 8.0（最優先）
- 短期記憶: 優先度 6.0
- 中期記憶: 優先度 3.0
- 長期記憶: 優先度 2.0
- 習熟済み: 優先度 1.0

---

### 2. 復習モード（Review）

#### アイコン・色
- **アイコン**: `arrow.clockwise`
- **色**: Orange (#FF9500)

#### 説明
> 短期記憶〜中期記憶段階の単語を重点的に復習します。記憶の定着を確実にするための復習モードです。

#### 推奨シーン
- ✅ テスト前の復習
- ✅ 一通り学習した後の定着確認
- ✅ 記憶が曖昧な単語を再確認したいとき
- ✅ 定期的なメンテナンス学習

#### 学習のコツ
- 💡 学習から数日〜1週間後に実施
- 💡 思い出すプロセスを大切に
- 💡 間違えた単語はメモして重点復習

#### 出題の特徴
- 未学習単語: 優先度 1.0（最低）
- 初回接触: 優先度 4.0
- 短期記憶: 優先度 8.0（最優先）
- 中期記憶: 優先度 7.0（優先）
- 長期記憶: 優先度 5.0
- 習熟済み: 優先度 2.0

---

### 3. 補習モード（Remediation）

#### アイコン・色
- **アイコン**: `star.fill`
- **色**: Red (#FF3B30)

#### 説明
> 苦手な単語や正答率の低い単語を集中的に学習します。記憶が不安定な初期段階の単語を徹底的に反復して定着させます。

#### 推奨シーン
- ✅ 特定の単語が覚えられないとき
- ✅ 正答率が低い単語を克服したいとき
- ✅ 苦手分野を集中的に学習したいとき
- ✅ 短期間で弱点を補強したいとき

#### 学習のコツ
- 💡 短時間（10-15分）を複数回に分ける
- 💡 間違えても焦らず、パターンを理解する
- 💡 関連する単語を一緒に覚える

#### 出題の特徴
- 未学習単語: 優先度 3.0
- 初回接触: 優先度 9.0（最優先）
- 短期記憶: 優先度 8.0（優先）
- 中期記憶: 優先度 6.0
- 長期記憶: 優先度 3.0
- 習熟済み: 優先度 0.5（最低）

---

## UI仕様

### データ構造

```swift
struct LearningModeRecommendationView: View {
    let mode: LearningMode
}
```

### レイアウト

```
┌─────────────────────────────────┐
│ 📖 通常                          │
│ 新しい単語と復習が必要な単語を... │
├─────────────────────────────────┤
│ このモードに適したシーン:         │
│ ✓ 日常的な学習                   │
│ ✓ 新しい単語帳を始めるとき        │
│ ✓ 学習ペースを安定させたいとき    │
│ ✓ 幅広く単語を覚えたいとき        │
├─────────────────────────────────┤
│ 学習のコツ:                      │
│ 💡 毎日コンスタントに学習する     │
│ 💡 新規単語と復習のバランスを保つ │
│ 💡 焦らず着実に進める            │
└─────────────────────────────────┘
```

---

## 実装詳細

### ファイル構成

```
Features/Quiz/Views/LearningModeRecommendationView.swift
```

### メインビュー

```swift
struct LearningModeRecommendationView: View {
    let mode: LearningMode

    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            // ヘッダー（アイコン + モード名）
            HStack {
                Image(systemName: modeIcon)
                    .font(.title2)
                    .foregroundColor(modeColor)
                Text(mode.displayName)
                    .font(.headline)
                    .foregroundColor(.primary)
            }

            // 説明文
            Text(modeDescription)
                .font(.subheadline)
                .foregroundColor(.secondary)
                .fixedSize(horizontal: false, vertical: true)

            Divider()

            // 推奨シーン
            VStack(alignment: .leading, spacing: 8) {
                Text("このモードに適したシーン:")
                    .font(.caption)
                    .foregroundColor(.secondary)
                    .textCase(.uppercase)

                ForEach(recommendedScenarios, id: \.self) { scenario in
                    HStack(alignment: .top, spacing: 8) {
                        Image(systemName: "checkmark.circle.fill")
                            .font(.caption)
                            .foregroundColor(.green)
                        Text(scenario)
                            .font(.subheadline)
                            .foregroundColor(.primary)
                    }
                }
            }

            // 学習のコツ（オプション）
            if !tips.isEmpty {
                Divider()

                VStack(alignment: .leading, spacing: 8) {
                    Text("学習のコツ:")
                        .font(.caption)
                        .foregroundColor(.secondary)
                        .textCase(.uppercase)

                    ForEach(tips, id: \.self) { tip in
                        HStack(alignment: .top, spacing: 8) {
                            Image(systemName: "lightbulb.fill")
                                .font(.caption)
                                .foregroundColor(.yellow)
                            Text(tip)
                                .font(.subheadline)
                                .foregroundColor(.primary)
                        }
                    }
                }
            }
        }
        .padding()
        .background(Color(uiColor: .secondarySystemGroupedBackground))
        .cornerRadius(12)
    }
}
```

### プロパティ計算

```swift
// モード別のアイコン
private var modeIcon: String {
    switch mode {
    case .normal: return "book.fill"
    case .review: return "arrow.clockwise"
    case .remediation: return "star.fill"
    }
}

// モード別の色
private var modeColor: Color {
    switch mode {
    case .normal: return .blue
    case .review: return .orange
    case .remediation: return .red
    }
}

// モード別の説明文
private var modeDescription: String {
    switch mode {
    case .normal:
        return "新しい単語と復習が必要な単語をバランスよく学習します。初見の単語は集中的に反復し、既習単語は適度な間隔で復習します。"
    case .review:
        return "短期記憶〜中期記憶段階の単語を重点的に復習します。記憶の定着を確実にするための復習モードです。"
    case .remediation:
        return "苦手な単語や正答率の低い単語を集中的に学習します。記憶が不安定な初期段階の単語を徹底的に反復して定着させます。"
    }
}

// 推奨シーンのリスト
private var recommendedScenarios: [String] {
    switch mode {
    case .normal:
        return [
            "日常的な学習",
            "新しい単語帳を始めるとき",
            "学習ペースを安定させたいとき",
            "幅広く単語を覚えたいとき"
        ]
    case .review:
        return [
            "テスト前の復習",
            "一通り学習した後の定着確認",
            "記憶が曖昧な単語を再確認したいとき",
            "定期的なメンテナンス学習"
        ]
    case .remediation:
        return [
            "特定の単語が覚えられないとき",
            "正答率が低い単語を克服したいとき",
            "苦手分野を集中的に学習したいとき",
            "短期間で弱点を補強したいとき"
        ]
    }
}

// 学習のコツのリスト
private var tips: [String] {
    switch mode {
    case .normal:
        return [
            "毎日コンスタントに学習する",
            "新規単語と復習のバランスを保つ",
            "焦らず着実に進める"
        ]
    case .review:
        return [
            "学習から数日〜1週間後に実施",
            "思い出すプロセスを大切に",
            "間違えた単語はメモして重点復習"
        ]
    case .remediation:
        return [
            "短時間（10-15分）を複数回に分ける",
            "間違えても焦らず、パターンを理解する",
            "関連する単語を一緒に覚える"
        ]
    }
}
```

---

## QuizSettingsViewへの統合

### 表示位置

```swift
// QuizSettingsView.swift内

VStack(alignment: .leading, spacing: 16) {
    SectionHeader(title: "学習モード")
    
    Picker("学習モード", selection: $settings.model.learningMode) {
        ForEach(LearningMode.allCases) { mode in
            Text(mode.displayName).tag(mode)
        }
    }
    .pickerStyle(.segmented)
    
    // モード別の推奨設定表示
    LearningModeRecommendationView(mode: settings.model.learningMode)
        .padding(.top, 8)
}
```

### onChange対応

```swift
// 学習モード変更時に推奨設定を更新
.onChange(of: settings.model.learningMode) {
    // ビューが自動的に更新される（再描画）
}
```

---

## プレビュー実装

```swift
#Preview("通常モード") {
    LearningModeRecommendationView(mode: .normal)
        .padding()
        .background(Color(uiColor: .systemGroupedBackground))
}

#Preview("復習モード") {
    LearningModeRecommendationView(mode: .review)
        .padding()
        .background(Color(uiColor: .systemGroupedBackground))
}

#Preview("補習モード") {
    LearningModeRecommendationView(mode: .remediation)
        .padding()
        .background(Color(uiColor: .systemGroupedBackground))
}

#Preview("全モード比較") {
    ScrollView {
        VStack(spacing: 16) {
            ForEach(LearningMode.allCases) { mode in
                LearningModeRecommendationView(mode: mode)
            }
        }
        .padding()
    }
    .background(Color(uiColor: .systemGroupedBackground))
}
```

---

## アクセシビリティ

### VoiceOver対応

```swift
.accessibilityElement(children: .combine)
.accessibilityLabel("\(mode.displayName)モード")
.accessibilityHint(modeDescription)
```

### Dynamic Type対応

すべてのテキストでDynamic Typeを使用：
- `.font(.headline)` - システムヘッドライン
- `.font(.subheadline)` - システムサブヘッドライン
- `.font(.caption)` - システムキャプション

---

## テスト観点

### 表示テスト

- [ ] 全モードのアイコンが正しい
- [ ] 全モードの色が正しい
- [ ] 説明文が完全に表示される
- [ ] 推奨シーンが4項目表示される
- [ ] 学習のコツが3項目表示される

### レイアウトテスト

- [ ] ライトモードで適切
- [ ] ダークモードで適切
- [ ] iPhone SE（小画面）で正常
- [ ] iPad（大画面）で正常
- [ ] 横向きで正常

### 動的動作

- [ ] モード切り替え時に即座に更新
- [ ] テキストが長い場合に自動折り返し
- [ ] スクロール可能

---

## パフォーマンス

### 計算コスト

- アイコン・色・テキストの計算: O(1)
- レンダリング: 軽量（静的コンテンツのみ）

### メモリ使用量

- 非常に小さい（文字列とアイコンのみ）
- キャッシュ不要

---

## トラブルシューティング

### 推奨設定が表示されない

**原因**: LearningModeRecommendationViewが配置されていない
**対処**: QuizSettingsView.swiftに追加

### アイコンが表示されない

**原因**: SF Symbolsの名前が間違っている
**対処**: システムシンボルブラウザで確認

### テキストが切れる

**原因**: `.fixedSize(horizontal: false, vertical: true)`が未設定
**対処**: 説明文に設定を追加

---

## 将来の拡張

### 予定されている機能

1. **詳細統計**: 各モードでの学習履歴を表示
2. **おすすめ判定**: 現在の学習状況から最適なモードを自動提案
3. **カスタムモード**: ユーザー独自の優先度設定
4. **学習効果分析**: モード別の学習効率を測定

---

## 関連ドキュメント

- `02_出題設定_仕様書.md` - 出題設定画面全体
- `09_出題ロジック_仕様書.md` - モード別の出題ロジック
- `docs/COMPREHENSIVE_SPECIFICATION.md` - データモデル詳細
- `Features/Study/Domain/MemoryStage.swift` - 記憶段階と優先度

---

## 復元手順

機能が喪失した場合の復元手順：

1. **LearningMode列挙型**を確認（Stores/QuizSettings.swift）
2. **LearningModeRecommendationView.swift**を作成（このドキュメントのコードを参考）
3. **QuizSettingsView.swift**に統合（Picker直後に配置）
4. プレビューで各モードの表示を確認
5. 実機で動作確認

---

**最終更新**: 2025-10-25  
**ファイル**: `Features/Quiz/Views/LearningModeRecommendationView.swift`  
**依存**: `LearningMode` (Stores/QuizSettings.swift)
