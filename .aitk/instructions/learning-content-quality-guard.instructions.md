---
description: 学習コンテンツ（問題データ・学習AI）品質保証 強制ガード
applyTo: '**/*.{json,ts,tsx,js,jsx,py}'
---

# 🚨 学習コンテンツ品質保証 強制ガード

## ⚠️ 最重要原則

**学習AIシステムと問題データは、生徒の学習効率と学習成果に直結する最重要部分です。**

- 絶対に手を抜かない
- 最大限の手間をかける
- 徹底的に検証する
- 妥協を許さない

---

## 📋 対象ファイル（絶対に慎重に扱うこと）

### 学習AIシステム
- `src/ai/**/*.ts` - すべてのAIコンポーネント
- `src/storage/progress/*.ts` - 進捗管理システム
- `src/hooks/useAdaptiveLearning.ts` - 適応型学習
- `src/hooks/useLearningEngine.ts` - 学習エンジン

### 問題データ
- `public/data/vocabulary/**/*.csv` - 語彙データ
- `public/data/grammar/**/*.json` - 文法問題データ
- `scripts/**/*.py` - データ生成・検証スクリプト

### UI/UX（学習体験）
- `src/components/TranslationView.tsx` - 翻訳タブ
- `src/components/SpellingView.tsx` - スペルタブ
- `src/components/GrammarQuizView.tsx` - 文法タブ
- `src/components/MemorizationView.tsx` - 暗記タブ

---

## 🛡️ 強制ルール（違反厳禁）

### ルール1: 修正前の完全な影響分析（必須）

```bash
# 修正前に必ず実行すること
1. 現在の仕様を完全に理解
2. 影響を受けるすべてのファイルをリストアップ
3. 学習AIとの整合性を確認
4. 既存データへの影響を評価
5. 生徒の学習体験への影響を予測
```

**違反例**:
- ❌ 1つのファイルだけを修正
- ❌ 影響範囲を調査せずに修正
- ❌ 「多分大丈夫」で進める

**正しい例**:
- ✅ 関連する全ファイルをリスト化
- ✅ 学習AIコンポーネント間の依存関係を図示
- ✅ データ構造の変更が他に影響しないか確認

---

### ルール2: データ品質の徹底検証（必須）

#### 問題データの品質基準

```typescript
// すべての問題は以下を満たすこと
interface QualityStandards {
  // 1. データ完全性
  hasId: true;                    // 一意なIDが必須
  hasCorrectAnswer: true;         // 正解が必須
  hasExplanation: true;           // 解説が必須（空でない）
  hasChoices: true;               // 選択肢が3つ以上（選択式問題の場合）
  
  // 2. 日本語品質
  japaneseIsNatural: true;        // 自然な日本語
  noEnglishInJapanese: true;      // 日本語フィールドに英語が混入していない
  
  // 3. 英語品質
  englishIsCorrect: true;         // 文法的に正しい英語
  hasProperSpacing: true;         // 適切なスペーシング
  
  // 4. 学習効果
  hasMnemonic: true;              // 覚え方・語源がある（語彙）
  hasUsageExample: true;          // 用例がある
  difficultyIsAppropriate: true;  // 難易度が適切
  
  // 5. 一貫性
  formatIsConsistent: true;       // 表記ルールに従っている
  categoryIsCorrect: true;        // カテゴリーが正しい
}
```

#### 必須検証スクリプト

```bash
# データ修正後に必ず実行
cd /path/to/project

# 1. データ整合性チェック
python3 scripts/check_passage_consistency.sh

# 2. 品質検証
npm run test:data-quality

# 3. 重複チェック
python3 scripts/check_duplicates.py

# 4. 表記ルール準拠チェック
python3 scripts/check_format_consistency.py
```

---

### ルール3: 学習AIシステムとの整合性確保（必須）

#### 変更時の確認事項

```typescript
// カテゴリー判定ロジックを変更する場合
const filesRequiringUpdate = [
  'src/storage/progress/progressStorage.ts',     // ✅ 更新必須
  'src/ai/simulation/learningAISimulator.ts',    // ✅ 更新必須
  'src/ai/scheduler/QuestionScheduler.ts',       // ✅ 更新必須
  'src/ai/specialists/MemoryAI.ts',              // ✅ 確認必須
  'src/hooks/useAdaptiveLearning.ts',            // ✅ 確認必須
  'docs/specifications/learning-ai-system-architecture.md', // ✅ 更新必須
];

// 優先度計算ロジックを変更する場合
const additionalFiles = [
  'src/ai/scheduler/AntiVibrationFilter.ts',     // ✅ 確認必須
  'src/strategies/memoryAcquisitionAlgorithm.ts', // ✅ 確認必須
];
```

#### 統一性チェックリスト

- [ ] すべてのAIコンポーネントで判定基準が統一されている
- [ ] シミュレーターの結果と実際の動作が一致する
- [ ] 4つのタブ（翻訳・スペル・文法・暗記）で動作が統一されている
- [ ] ドキュメントと実装が一致している
- [ ] テストケースがすべてパスする

---

### ルール4: 段階的実装と検証（必須）

#### 実装プロセス

```
Phase 1: 設計・影響分析
  ├─ 現状の完全な理解
  ├─ 変更内容の明確化
  ├─ 影響範囲の特定
  └─ リスク評価

Phase 2: 試験的実装（小規模）
  ├─ 1つのコンポーネントで試験実装
  ├─ ユニットテスト作成
  ├─ 動作確認
  └─ 問題がなければ次へ

Phase 3: 統一的実装（全体）
  ├─ 関連する全ファイルを同時に修正
  ├─ 統合テスト実行
  ├─ シミュレーションで検証
  └─ 問題がなければ次へ

Phase 4: データ検証
  ├─ 既存データへの影響確認
  ├─ データ整合性チェック
  ├─ 品質基準の確認
  └─ 問題がなければ次へ

Phase 5: ドキュメント更新
  ├─ 仕様書更新
  ├─ CHANGELOG記録
  ├─ コメント追加
  └─ 完了

Phase 6: 最終検証
  ├─ 全テスト実行
  ├─ 手動テスト（実際に学習してみる）
  ├─ パフォーマンス確認
  └─ リリース準備完了
```

**絶対に飛ばしてはいけないフェーズ**: すべて

---

### ルール5: テスト・検証の義務化（必須）

#### 必須テスト項目

```bash
# 1. ユニットテスト（修正箇所）
npm run test -- --grep="カテゴリー|category|mastered"

# 2. 統合テスト（全体）
npm run test:integration

# 3. 学習AIシミュレーション（設定タブ）
# → ブラウザで設定タブを開き、「学習AIシミュレート」を実行
# → 100回試行して、カテゴリー分布を確認

# 4. 実際の学習体験テスト
# → 翻訳・スペル・文法・暗記の各タブで実際に学習
# → カテゴリー判定が正しいか確認
# → 出題優先度が適切か確認

# 5. データ品質テスト
python3 scripts/validate_all_data.py

# 6. パフォーマンステスト
npm run test:performance
```

#### 検証チェックリスト

- [ ] すべてのユニットテストがパス
- [ ] 統合テストがパス
- [ ] シミュレーション結果が期待通り
- [ ] 実際の学習で問題がない
- [ ] データ品質基準を満たしている
- [ ] パフォーマンスが劣化していない
- [ ] 既存データに悪影響がない
- [ ] ドキュメントが更新されている

---

### ルール6: 問題データ作成・修正の品質基準（必須）

#### 語彙データ（CSV）

```csv
# 必須フィールド
word,meaning,pronunciation,category,difficulty,example,etymology,usage_notes

# 品質基準
1. 意味: 自然な日本語、英語混入なし、番号付きで複数の意味
2. 発音: IPA記法、CMU音素記法の両方
3. カテゴリー: 統一された分類（名詞、動詞、形容詞等）
4. 例文: 実用的で理解しやすい
5. 語源: 丁寧で覚えやすい解説（必須）
6. 用法注意: よくある間違いの指摘
```

#### 文法問題データ（JSON）

```json
{
  "id": "必須（一意）",
  "type": "fillInBlank | verbForm | sentenceOrdering",
  "difficulty": "beginner | intermediate | advanced",
  "sentence": "問題文（穴埋め位置は____）",
  "correctAnswer": "正解（必須）",
  "choices": ["3つ以上の選択肢（選択式の場合必須）"],
  "explanation": "詳細な解説（必須、空でない）",
  "baseForm": "動詞の原形（verbFormの場合）",
  "hint": "ヒント（任意だが推奨）"
}
```

#### 品質チェック項目

- [ ] すべての必須フィールドが存在
- [ ] IDが一意
- [ ] 正解が正しい
- [ ] 選択肢が適切（引っ掛け問題として学習効果がある）
- [ ] 解説が丁寧で分かりやすい
- [ ] 日本語が自然
- [ ] 英語が文法的に正しい
- [ ] 難易度が適切
- [ ] 表記ルールに従っている

---

### ルール7: ドキュメント更新の義務化（必須）

#### 更新必須ドキュメント

```
変更内容に応じて以下を更新：

1. 仕様書
   └─ docs/specifications/learning-ai-system-architecture.md

2. CHANGELOG
   └─ CHANGELOG.md または CHANGELOG_ADAPTIVE_LEARNING.md

3. README
   └─ 関連機能の説明を更新

4. コード内コメント
   └─ 判定ロジック、計算式に詳細なコメント

5. データ仕様書
   └─ docs/specifications/data-format.md（語彙・文法データの構造）
```

#### コメント記述基準

```typescript
/**
 * カテゴリー判定ロジック（4タブ統一）
 * 
 * 判定基準:
 * - mastered（覚えてる・定着済）:
 *   1. 1発正解（totalAttempts === 1 && correctCount === 1）
 *   2. 連続3回正解（consecutiveCorrect >= 3）
 * 
 * - incorrect（分からない・要復習）:
 *   incorrectCount > 0 && consecutiveCorrect < 2
 * 
 * - still_learning（まだまだ・学習中）:
 *   上記以外
 * 
 * - new（新規）:
 *   totalAttempts === 0
 * 
 * @param wordProgress 単語の進捗データ
 * @returns カテゴリー
 * @see docs/specifications/learning-ai-system-architecture.md
 */
function determineCategory(wordProgress: WordProgress): Category {
  // 実装
}
```

---

## 🚫 絶対にやってはいけないこと

### 1. 安易な修正
- ❌ 「とりあえず動けばいい」という考え
- ❌ 影響範囲を考えずに修正
- ❌ テストをスキップ

### 2. 不完全な実装
- ❌ 一部のファイルだけ修正して放置
- ❌ ドキュメント更新をサボる
- ❌ コメントを書かない

### 3. データ品質の妥協
- ❌ 「だいたい合ってればいい」
- ❌ 検証をスキップ
- ❌ 不自然な日本語を許容

### 4. 学習AIシステムの軽視
- ❌ 複数のAIの協調関係を無視
- ❌ シミュレーションと実動作の不一致を放置
- ❌ カテゴリー判定基準の不統一

---

## ✅ 正しい開発プロセス

### Step 1: 徹底的な理解
```bash
# 現状を完全に理解する
1. 関連ファイルをすべて読む
2. 学習AIアーキテクチャ仕様書を読む
3. 既存のテストケースを確認
4. シミュレーターで現在の動作を確認
5. 実際に学習して動作を体験
```

### Step 2: 詳細な設計
```bash
# 変更内容を明確にする
1. 何を変更するか
2. なぜ変更するか
3. どこに影響するか
4. どうやって検証するか
5. ロールバック方法は何か
```

### Step 3: 慎重な実装
```bash
# 段階的に実装する
1. 小規模な試験実装
2. ユニットテスト作成
3. 動作確認
4. 全体への展開
5. 統合テスト
```

### Step 4: 徹底的な検証
```bash
# すべてのテストを実行する
1. ユニットテスト
2. 統合テスト
3. シミュレーション
4. 手動テスト
5. データ品質チェック
6. パフォーマンステスト
```

### Step 5: 完全なドキュメント化
```bash
# ドキュメントを更新する
1. 仕様書更新
2. CHANGELOG記録
3. README更新
4. コメント追加
5. レビュー
```

### Step 6: 最終確認
```bash
# リリース前の最終チェック
1. すべてのテストがパスしているか
2. ドキュメントは更新されているか
3. 品質基準を満たしているか
4. 既存データに問題はないか
5. 学習体験が向上しているか
```

---

## 🎯 品質目標（妥協禁止）

### データ品質
- **目標**: すべての問題が品質基準を100%満たす
- **基準**: 解説が丁寧、選択肢が適切、日本語が自然、英語が正しい

### 学習AIシステム
- **目標**: すべてのAIコンポーネントが協調して動作
- **基準**: カテゴリー判定が統一、シミュレーションと実動作が一致

### 学習体験
- **目標**: 生徒が効率的に学習できる
- **基準**: 適切な問題が適切なタイミングで出題される

### コード品質
- **目標**: 可読性が高く、保守しやすい
- **基準**: コメントが充実、テストが完備、ドキュメントが最新

---

## 📊 品質メトリクス（定期確認）

### 週次チェック
- [ ] すべてのテストがパスしているか
- [ ] データ品質基準を満たしているか
- [ ] ドキュメントが最新か
- [ ] 学習AIシミュレーションが正常か

### 月次レビュー
- [ ] 生徒の学習効率は向上しているか
- [ ] 新しい問題の品質は高いか
- [ ] 学習AIの精度は向上しているか
- [ ] パフォーマンスは良好か

---

## 🆘 問題が発生した場合

### 優先順位
1. **P0（緊急）**: 生徒の学習が止まる問題
2. **P1（高）**: 学習効率が著しく低下する問題
3. **P2（中）**: データ品質の問題
4. **P3（低）**: UI/UXの改善

### 対応プロセス
```bash
1. 問題の特定と影響範囲の確認
2. 緊急対応（必要に応じてロールバック）
3. 根本原因の分析
4. 恒久対策の実装
5. 再発防止策の策定
6. ドキュメント更新
```

---

## 💡 覚えておくべきこと

1. **生徒の学習が最優先**
   - すべての判断は「生徒の学習効率と成果」を基準にする
   
2. **品質に妥協しない**
   - 「だいたい」「おそらく」「多分」は禁止
   
3. **システム全体を理解する**
   - 一部だけ見て判断しない
   
4. **テストは義務**
   - テストなしのリリースは絶対にしない
   
5. **ドキュメントは資産**
   - 更新しないドキュメントは負債になる

---

**このガードを遵守することで、生徒に最高の学習体験を提供できます。**
